import {createRequire} from 'node:module';import {pathToFileURL,fileURLToPath} from 'node:url';import path from 'node:path';import fs from 'node:fs/promises';import assert from 'node:assert/strict';
const require=createRequire('${HOME}/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/package.json');const{chromium}=require('playwright');const root=path.dirname(fileURLToPath(import.meta.url));const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const p=await browser.newPage();const network=[],errors=[];p.on('request',r=>{if(/^https?:/.test(r.url()))network.push(r.url())});p.on('pageerror',e=>errors.push(e.message));await p.goto(pathToFileURL(path.join(root,'package-check/Wringy-Design-Reference/index.html')).href);await p.evaluate(()=>document.fonts.ready);assert.equal(await p.locator('[data-family]').count(),31);assert.equal(await p.evaluate(()=>document.fonts.check('16px Manrope')&&document.fonts.check('16px "Noto Sans SC"')),true);await p.locator('[data-theme-toggle]').click();assert.equal(await p.locator('html').getAttribute('data-theme'),'dark');assert.deepEqual(network,[]);assert.deepEqual(errors,[]);const result={status:'passed',unpackedFileURL:true,families:31,fontsLoaded:true,themeSwitch:true,externalRequests:network,errors};await fs.writeFile(path.join(root,'package-verification.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result));await browser.close();
