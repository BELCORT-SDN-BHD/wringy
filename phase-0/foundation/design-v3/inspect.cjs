const {chromium}=require('${HOME}/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const path=require('path');(async()=>{const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('file://'+path.join(__dirname,'showcase.html'));await p.evaluate(()=>document.fonts.ready);await p.emulateMedia({media:'print'});console.log(JSON.stringify(await p.locator('.sheet').evaluateAll(es=>es.map(e=>({id:e.id,header:e.querySelector('header').getBoundingClientRect().toJSON(),cols:[...e.querySelectorAll('.rulegrid>p')].map(x=>({width:x.getBoundingClientRect().width,scroll:x.scrollWidth,font:getComputedStyle(x).fontFamily})),font:document.fonts.check('16px "Noto Sans SC"')}))),null,2));await b.close()})();
