# Project Progress

## Current State

- 2026-09-22: `main` carries the owner's self-edits through [PR #72](https://github.com/BELCORT-SDN-BHD/wringy/pull/72): the orchestrator-fable skill line change, Codex interface metadata added under the Claude skill, the Codex-facing orchestrator variant removed, and AGENTS.md working-protocol item 4 shortened. Use `git log -1` for the current checkout commit.
- Agent tooling: 25 Matt Pocock skills vendored for both agents, the Graphify Claude hooks and Codex skill / hook, and the owner's orchestrator-fable skill for both agents. A read-only setup audit on 2026-09-22 found both installations sound; the owner ruled on its findings the same day (applied ones under Completed, accepted quirks and the one pending action under Known Issues).
- 2026-09-22: M1 three-role prototype built on branch `feat/m1-prototype` / [PR #77](https://github.com/BELCORT-SDN-BHD/wringy/pull/77): `apps/web` (Next.js 16.3.5 App Router, official shadcn radix-nova components, next-intl en-MY / ms-MY / zh-Hans-MY, zustand persist, pure-TypeScript demo engine under `src/domain`). Kickoff record, demo script, executed P01–P11 acceptance record and known issues live in [docs/m1-prototype](m1-prototype/kickoff.md). Gates on the final tree: lint and typecheck clean, 322 Vitest tests, 318 Playwright tests passed with 33 viewport-scoped skips across 390 / 1440 / 320, production build OK; `web` and `planning` CI jobs green. **Founder acceptance recorded 2026-09-23 on [#9](https://github.com/BELCORT-SDN-BHD/wringy/issues/9): PASSED** after a guided walk of the main flow and all five exception scenarios in the founder's own Chrome plus their own browse; #1–#9 and milestone 1 are closed (spec #1 closed 2026-09-23 on that acceptance). Owner rulings applied in [PR #78](https://github.com/BELCORT-SDN-BHD/wringy/pull/78), graph refreshed in [PR #79](https://github.com/BELCORT-SDN-BHD/wringy/pull/79). A shareable copy of `docs/m1-prototype` (with a colleague-facing README) was placed at `~/Desktop/wringy-m1-demo/` on the owner's machine (not tracked).
- 2026-09-23: [PRD](PRD.md) §5 and [Architecture](ARCHITECTURE.md) §1 implemented-state sections rewritten in place for the accepted M1 prototype (`implementation_status: m1-prototype-accepted; product-runtime-unbuilt`), PR #81. GitHub owns live execution state.
- 2026-09-23: M2-00 gate ([#14](https://github.com/BELCORT-SDN-BHD/wringy/issues/14)) passed. The kickoff package [docs/m2-internal/kickoff-package.md](m2-internal/kickoff-package.md) (M1 evidence register, governance audit, identity / session / migration / dependency / test / account / workspace proposals) was reviewed by the founder, who ruled on all 33 decisions and 20 gaps in the session (§9.1, §11). Notable rulings: self-service merchant orgs, email invitations (7 days), ops capabilities by audited bootstrap script, tester allowlist on the deployed internal build, company Google Workspace owns the vendor accounts, Supabase Free then Pro with two projects, three Render Starter instances, Node 24 + embedded-postgres 17, web CI checks become required once M2-01 lands, no enforced cross-vendor review (orchestrator's discretion), founder walks M2-AC10 personally.
- 2026-09-23: M2-01 ([#16](https://github.com/BELCORT-SDN-BHD/wringy/issues/16)) **delivered and closed**: merged to `main` as `0f0a5bd` ([PR #82](https://github.com/BELCORT-SDN-BHD/wringy/pull/82)); Node 24.21.0 pin; `packages/{config,contracts,db}` (migrations 0001–0007, the pg-boss schema, embedded PostgreSQL 17 locally); the Fastify API (`/health`, `/internal/campaigns`, `/internal/worker-health`, read-only runtime role); the pg-boss worker (heartbeat, queue round trip, graceful drain); `/internal` in three locales reading the API; CI `app.yml` (`check`, `integration` on `postgres:17`, `e2e`, `images` with `push: false`). Waves W0–W4, then the W5 adversarial review: 23 findings confirmed, all fixed in 18 commits, none skipped. CI is green on every PR head (`9ca00a5`, `12a85ab`, `fe17df4`, `3153ed3`: runs 35807628772, 35825491353, 35828034405, 35829822317) and on `main` after the merge (run 35831482658). Ruling D22 applied the same day: `main` now requires `planning`, `check`, `integration` and `e2e` (strict; re-read into #16). The `code-review` skill ran both axes against `main` (Spec: no scope creep), and a Codex read-only cross-vendor review ran on `12a85ab` (saved on PR #82); its two defects, the production relabel race and query strings in request logs, are fixed in `2e84a7a` and `fe17df4` with tests. Evidence in the [acceptance record](m2-internal/acceptance-record.md) and [known issues](m2-internal/known-issues.md): an internal-build acceptance, not a business acceptance. The knowledge graph was refreshed for the M2-01 tree at clock-out (9,623 nodes, 481 named communities; `docs/m2-internal/screenshots/` and the acceptance-mapping self-test fixtures excluded via `.graphifyignore`, like the M1 screenshots).

## Completed

- Blueprints: the owner's focus criteria live in each file's YAML frontmatter; blueprints are updated in place after accepted Wayfinder / to-spec decisions and before to-tickets. AGENTS, README, CONTEXT and planning / ADR / foundation navigation point at the two blueprints. Verified 2026-09-21: valid YAML, all 88 local blueprint links / anchors, `git diff --check`, and `python -X utf8 scripts/check-planning.py` (5 specs, 60 tasks, acyclic). Product runtime tests were not run for documentation-only changes.
- Matt Pocock skills: the 25 published skills are installed into each of `.agents/skills` and `.claude/skills`, pinned to upstream `c55ee46073ed923f86ce59a5eb3b6d895095d1b7` (1.2.3); see the [installation and update record](agents/matt-pocock-skills.md). Re-verified 2026-09-22: 74 of 74 files per agent match the upstream Git blob hashes, the pin equals upstream `main` HEAD, all 50 skill frontmatters and their 50 Codex metadata files parse, the five triage labels exist on GitHub, and the project-scoped disablement of the managed Claude plugin matches the installed plugin id, so no skill loads twice. Fourteen skills are user-invoked only by upstream design (`disable-model-invocation`), so they are absent from the model's own skill list.
- Graphify: CLI `0.9.65` is the latest release. The Claude hooks in `.claude/settings.json` and the Codex hook in `.codex/hooks.json` match what the installed package writes, and direct command tests return valid soft `PreToolUse` JSON. `.agents/skills/graphify` is content-identical to the package's `0.9.65` Codex skill and eight references (version stamp corrected from `0.9.63` on 2026-09-22); Claude Code uses the global `~/.claude/skills/graphify` at `0.9.65`. The git hooks and the `graph.json` merge driver are installed in this clone. Do not run the general Graphify installer to update the project files: it also writes instruction files and targets `.codex/skills`.
- Graph: refreshed on 2026-09-22 with the incremental `/graphify . --update` flow for the instruction and navigation documents changed since the last semantic pass; [GRAPH_REPORT](../graphify-out/GRAPH_REPORT.md) holds the current counts.
- Orchestration is owner configuration: the Claude skill's Codex lane prefers `gpt-6-astra` / `medium`. Codex has its own orchestrator-fable skill again at `.agents/skills/orchestrator-fable` (the owner's Codex-facing variant, restored on the owner's decision of 2026-09-22 with the same description edit as the Claude copy); both copies carry a valid `agents/openai.yaml` with `allow_implicit_invocation: false`: the owner wants Codex to run it only when named.
- Line endings: `.gitattributes` sets `* text=auto eol=lf`, so working-tree bytes equal the committed blobs and Graphify's content hashes no longer depend on the machine; the manifest was restamped for LF bytes without blanking any semantic hash. `graphify-out/cost.json` is untracked per upstream guidance, and README explains what a fresh clone must install (`graphify hook install`, the global Claude skill, the Codex hook trust prompt).

- M1 prototype (2026-09-22): built in four waves — scaffold and domain contract; demo engine (236 tests at landing) and app shell in parallel; merchant, creator and operations/finance workspaces in isolated worktrees; deadlines/closure, guided `/demo` entry, acceptance suite and records. An adversarial review workflow (11 reviewers, three refuters per finding) raised 44 findings, confirmed 31 and fixed 27 at the cause with source-rule citations; 4 partial skips are owner decisions recorded in [known-issues](m1-prototype/known-issues.md) ("Rules the prototype records but does not simulate"). Graph excludes `docs/m1-prototype/screenshots/` (evidence artefacts) via `.graphifyignore`.

- 2026-09-25/26: M2-02 ([#21](https://github.com/BELCORT-SDN-BHD/wringy/issues/21), Google sign-in, refresh, sign-out) **built and reviewed on `feat/m2-02`, [PR #86](https://github.com/BELCORT-SDN-BHD/wringy/pull/86)**; the ticket stays open for the founder's real-login walk. Shape: Understand (5 readers) → design record [m2-02-code-review.md](m2-internal/m2-02-code-review.md) → 3-lens design critique (rev 2) → W1 foundation → W2 api ‖ web ‖ e2e-harness in worktrees → W3 integration → W4 real-session probe → W5 adversarial review (47 → 31 confirmed → 29 fixed, 2 queued) → `/code-review` + a native independent review in place of Codex (usage limit) → W6 (16 findings fixed) → rev 3 of the design record. Delivered: migrations 0008–0010 (`profiles`, `sign_in_allowlist`, column grants), the `platform.session_is_live` bootstrap, `pnpm db:allowlist` / `db:platform-bootstrap`, the Fastify identity routes and the two liveness adapters, `proxy.ts` and the `/auth/*` handlers, the `/internal/sign-in` page in three languages, a fake Supabase Auth server for Playwright, `check:supabase-scope`, rule (d) of `check:acceptance`. Gates at `3f50af4`: 670 unit, db int 73, api int 57 (+1 win32 skip), worker int 15, `e2e:internal` 64 passed / 4 declared skips, M1 suite 327 / 33 unchanged. Real evidence without Google: both Supabase projects verified ES256 + Google-only + `scope=email profile`; a real dev-project session (admin-minted) verified by the API, refused with `session.revoked` after a real logout. Record: [acceptance-record.md](m2-internal/acceptance-record.md) §M2-AC02 (simulated rows labelled; 9 Real rows NOT EXECUTED); [known-issues.md](m2-internal/known-issues.md) §M2-02; the founder's walk: [m2-02-real-login-runbook.md](m2-internal/m2-02-real-login-runbook.md).

## In Progress

- PR #86 (`feat/m2-02`) awaiting CI and merge; after the merge the knowledge graph needs its clock-out refresh (52 commits, ~150 files) and #21 gets the status comment. The founder's vendor accounts exist and their values are in the local, gitignored `.env.vendors` (all filled except `RENDER_API_KEY`, which M2-09 needs); `apps/api/.env` and `apps/web/.env.local` were generated from it for the real-login walk (internal mode; the M1 suite pins demo mode itself).

## Known Issues

- Accepted by the owner on 2026-09-22 (do not re-flag): `.claude/skills/orchestrator-fable/SKILL.md` names `/loop-me`, an upstream in-progress skill that is not installed, and prescribes `--effort`, which the Codex plugin lane accepts but `codex exec` rejects; AGENTS.md working-protocol item 8 names the labels `idea` and `need-triages` while GitHub has `needs-triage` and no `idea`, and `docs/agents/issue-tracker.md` names `wayfinder:map`, which does not exist. The Codex orchestrator variant also names worker models `Codex-sonnet-5` / `Codex-opus-5`, owner-authored placeholders.
- The Codex project hook has no trust record in `~/.codex/config.toml`: approve its trust prompt in the Codex app yourself. Direct hook-command tests do not prove automatic host dispatch.
- On Windows the planning checker requires UTF-8 mode (`python -X utf8 scripts/check-planning.py`); its default-codepage JSON read is an existing portability issue.
- Product runtime beyond the M1 simulation, real provider / payment capability, capacity and recovery remain unverified. Blueprint completion does not fulfil stage implementation or release gates; M3 must re-verify every money rule in a server transaction.
- M1 owner decisions all ruled on 2026-09-22 ([#9](https://github.com/BELCORT-SDN-BHD/wringy/issues/9); recorded in [known-issues](m1-prototype/known-issues.md) and summarised in [kickoff](m1-prototype/kickoff.md) "Owner rulings (2026-09-22)"): (1) **decided — accepted as recorded**: the cross-platform independent-cap switch records the merchant's permission only and changes no amount in M1, because submissions carry no cross-platform content identity; making the flag load-bearing needs a content-identity decision. (2) **decided — changed**: a pending-case deadline extension is granted only when the submission still has a claimable remainder when the block clears; with nothing claimable no deadline moves and no `deadline.claim_deadline_extended` notification is sent (`maybeGrantPendingCaseExtension` asks `evaluateClaimRequest` with the deadline gate lifted). The data-outage extension is unchanged. (3) **decided — kept**: finally rejected amounts stay deducted from what is newly claimable, while the reservation itself still returns to the pool. Malay and Chinese copy is a key-parity draft, not native-reviewed. Chromium-only test evidence.
- The tracked `.claude/settings.json` carries the machine-absolute graphify hook path the installer wrote (#76); a clone on another machine must run `graphify hook install` to rewrite it (an untracked `.claude/settings.json.graphify-bak` on the owner's machine holds the portable `graphify hook-guard` form).
- M2-01's limitations and unverified items are in [docs/m2-internal/known-issues.md](m2-internal/known-issues.md): no identity and no writes yet, fixture data only, Supabase and Render behaviours unverified, single-worker assumptions, and Windows-only local evidence (Linux paths proven in CI). D22 is applied; the Codex cross-vendor review ran on PR #82 (D23, discretionary).
- Supabase region deviation accepted by the founder on 2026-09-23: the dev project runs in `ap-northeast-2` (Seoul) and the staging project in `ap-northeast-1` (Tokyo), not the Singapore region rulings D16/D17 named. No product effect; only latency to Render Singapore and the data-location question for M4/M5, when a production project is created afresh. Both projects use ES256 signing keys and the new `sb_publishable_` / `sb_secret_` keys.
- Intermediate commits on `feat/m1-prototype` bypassed the graphify post-commit hook (`-c core.hooksPath=/dev/null`) to avoid label churn mid-build; the graph was refreshed once at clock-out with the incremental runbook.
- M2-02 (2026-09-26): the Codex cross-vendor review could not run (account usage limit until 2026-09-30); a native independent review ran in its place on the founder's instruction and is registered in the acceptance record. Local-only traps recorded in [known-issues.md](m2-internal/known-issues.md) §M2-02: the M1 suite at 12 workers can time out on this machine (6 is stable); `apps/web/tests/e2e/i18n.spec.ts`'s placeholder guard reads the runner's `API_INTERNAL_URL`, so run the M1 suite with it exported when `.env.local` sets it. Every M2-AC02 Real row is NOT EXECUTED until the founder's walk; the simulated rows cannot close #21.

## Next Steps

- Future accepted decisions replace affected blueprint sections using each file's frontmatter; do not append session history.
- **Founder (M2-02 close-out):** walk [m2-02-real-login-runbook.md](m2-internal/m2-02-real-login-runbook.md) (about 15 minutes with the four Google test users already on the local allow-list), record the Real rows in the acceptance record, then close #21. Two engineering rulings are queued in [known-issues.md](m2-internal/known-issues.md) §M2-02 (token-2: whether a JWKS with no matching key should be a 503 during a key rotation instead of a 401; liveness-2: whether Mechanism B may ask the Auth server before the transaction opens so a slow Auth server cannot hold pooled connections) — neither blocks M2-03. Retry the Codex read-only review on `main` once the account's limit resets (after 2026-09-30), or fold it into M2-10.
- **M2-03** ([#26](https://github.com/BELCORT-SDN-BHD/wringy/issues/26), orgs, memberships, capabilities, the audit log) is the next ticket: it builds on `profiles`, `request.actor`, `requireLiveSession` and the allow-list; kickoff §3.2–3.4 and D1–D12 are its contract. Keep the M2-02 shape (design record → critique → waves → adversarial review → `/code-review`).
- ARCHITECTURE §1 `implementation_status` still says `identity-and-saved-user-data-unbuilt`; refresh it (and PRD §5 if affected) once #21 closes on the founder's walk, per the blueprint maintenance frame.
- [M4 feasibility / #15](https://github.com/BELCORT-SDN-BHD/wringy/issues/15) can proceed in parallel after checking live dependencies.
