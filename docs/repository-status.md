# Repository governance status

2026-09-16. Planning workspace migration, not a product launch.

Root AGENTS.md and engineering-skill configuration are present. Business modules, runtime tests, deployments, rollback and module rules are implementation work. Dependency-direction, acceptance-to-test mapping, cross-vendor review and production approval enforcement must be wired before claiming their protection. No runtime acceptance has been executed by this migration.

Public migration excludes credentials, caches, personal account data and third-party reference assets; originals stay in the source workspace. Historical planning remains evidence, not active task state. GitHub Issues owns execution status.

Founder authorised all-stage planning and public publication on 2026-09-16. Earlier just-in-time planning statements are superseded. External validation and production approval gates remain. Exact implementation approval is recorded in each spec; newly synthesised scope does not invent historical approval.

## Bootstrap and inherited rules

This is a new repository: the initial documentation commit bootstraps main once. Subsequent implementation uses separate worktrees and PRs. The repository rules supplement, rather than weaken or replace, the founder's global AGENTS instructions. Existing organisation project-template has CI/deploy/gates workflows; adapting its runtime harness is tracked foundation work, not silently claimed installed. No deployment pipeline is enabled by this migration.
