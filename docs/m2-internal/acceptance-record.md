# M2 internal build — acceptance record (M2-AC01)

The evidence for [M2-01 / #16](https://github.com/BELCORT-SDN-BHD/wringy/issues/16), acceptance
M2-AC01/1–3 ([m2-spec.md](../planning/specs/m2-spec.md#m2-ac01),
[m2-01.md](../planning/tickets/m2-01.md)). Every row carries the spec's columns (`m2-spec.md` L49;
[kickoff-package.md](kickoff-package.md) §6.4): 验收ID, 版本, 环境, 测试或人工步骤, 期望, 实际, 时间,
证据引用, 未决项. A row that was not executed says **NOT EXECUTED** and why, which is the M1 rule
([M1 record](../m1-prototype/acceptance-record.md) L5). Each row says whether the verification was
simulated, sandbox or real.

Automated tests carry their acceptance ID in their full name (describe chain plus title). Manual and
governance evidence lives in the rows below. `pnpm check:acceptance`
(`scripts/check-acceptance-mapping.mjs`, CI job `check`) requires every M2 test name to carry
`M2-AC`, and every M2-AC01 sub-item to have a test name or a row here whose first cell starts with
its ID.

**M2-AC02 is recorded here too** ("M2-AC02 — Google sign-in, session refresh and sign-out" below),
and under a stricter rule: its spec forbids closing on simulated results, so every one of its
sub-items must have a row here of its own, and the check refuses the ticket otherwise. Its Real
rows are recorded up front as **NOT EXECUTED**.

**This is an internal-build acceptance, not a business acceptance.** It records what the internal
build does on a developer machine and in CI. A fixture campaign read through `/internal` shows that
the data path works; it proves no business rule. It is not a release either: M2 has no production
deployment (M2-AC09/3, `m2-spec.md` L66). And it is not the founder's acceptance: the founder walks
M2-AC10 personally on phone and desktop on staging (ruling D27, [kickoff-package.md](kickoff-package.md)
§9.1), and that walk belongs to M2-10. What the build deliberately does not do, and the limitations
accepted on the way, are in [known-issues.md](known-issues.md).

## Execution

| | |
|---|---|
| Recorded | 2026-09-23. The W0–W3 rows were consolidated by the W5 record worker from the eight W0–W3 worker reports (84 raw rows, deduplicated into the rows below); the W4 rows by the W4 worker; the review-fix rows and the recovery rehearsal by the W5 review-fix worker; the CI rows, the review summary and this consolidation by the W5 record worker (13:48 +08:00) |
| Branch | `feat/m2-01`, draft PR [#82](https://github.com/BELCORT-SDN-BHD/wringy/pull/82). GitHub holds `fe17df4` (`git ls-remote origin feat/m2-01`, 2026-09-23); CI ran green on `9ca00a5`, on `12a85ab` (the 18 review-fix commits plus this record) and on `fe17df4` (the cross-vendor fixes) |
| Executed by | Agent runs of M2-01: the W0–W1 worker; the W2 db, api and worker builders, the W2 integration and the W2 fix; the W3 build and the W3 fix; the W4 worker; the W5 review-fix worker; the W5 record worker. No founder step is recorded here |
| Runtime | Node 24.21.0 (root `.npmrc` `use-node-version`, `.nvmrc`), pnpm 10.33.0; PostgreSQL 17.10 through embedded-postgres 17.10.0-beta.17 locally; CI: `ubuntu-24.04` runners with a `postgres:17` service |
| Machine | Windows 11 (local); no Docker on this machine, so the images are built only in CI |
| Migration head | `0005_pgboss_grants` from `d60f579` to `9ca00a5`; `0007_data_origin_immutable` from the review fixes (`packages/db/src/expected-head.ts` L14) |
| Commands (local) | Per wave, in "W0–W3 gate runs", "W4 gate run" and "Review-fix gate run" below |
| Raw logs | The W0–W3 workers' command logs lived in the session scratchpad and are not tracked. Each row cites the test file and name, the commit and the command, which reproduce it |
| Review fixes | 2026-09-23: the W5 adversarial review's 23 confirmed findings fixed in the commits "Fix M2-01 review: …" from `18099af` to `9615439`; see "W5 adversarial review" |
| CI | [App checks run 35807628772](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772) and [Planning checks run 35807628875](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628875) on `9ca00a5`: check, integration, e2e, images and planning all **pass** (`gh pr checks 82`, 2026-09-23). They ran on the `pull_request` event, so on GitHub's merge commit `a6d7416` (the images job's `SHA` line). The review-fix head `12a85ab`: [App checks run 35825491353](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35825491353) and [Planning checks run 35825491377](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35825491377), all five jobs **pass** (check 1m55s, integration 1m24s, e2e 13m1s, images 2m11s, planning 11s). The cross-vendor-fix head `fe17df4`: [App checks run 35828034405](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35828034405) and [Planning checks run 35828034315](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35828034315), all five jobs **pass** (`gh run view 35828034405 --json conclusion,jobs`, 2026-09-23). This re-record commit changes documentation only; its own run is cited on PR #82 and #16 |

## M2-AC01/1 — M1 handoff register

Spec: "核对M1-08实际体验确认和P01–P11证据；登记产品仓库、模块归属、规则/设计审批引用及未完成项，不以设计系统展示替代原型"
(`m2-01.md`, Acceptance criteria).

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/1 register | `3def287` on `main` (PR #81 squash); no migration | Repository documents; real (no simulation) | Manual: check the M1-08 founder confirmation and the P01–P11 evidence; register the product repository, module ownership, rule and design approval citations and the open items; correct the stale M1 records | A register a reviewer can check line by line, with the prototype itself (not the design-system showcase) as the evidence | Done in [kickoff-package.md](kickoff-package.md) §1: M1 milestone closed with 9 closed and 0 open issues (§1.1); founder acceptance "PASSED" on #9 (§1.1); every P01–P11 row re-checked against `apps/web/tests/e2e/acceptance.spec.ts`, its unit tests and screenshots (§1.2); module ownership (§1.4); seven rule and design approvals with dates (§1.5); every inherited M1 item with its landing ticket (§1.6). The M1 record's stale lines were corrected in the same PR: [acceptance-record.md](../m1-prototype/acceptance-record.md) "Supporting suites" now reads 322 Vitest and 318 Playwright passed, and "Limitations" reads "Founder confirmation: EXECUTED, PASSED (2026-09-23)". The P01–P11 evidence is the `apps/web` prototype's own suite and screenshots, not the design-system showcase | 2026-09-23, orchestrator (M2-00 preparation); PR #81 merged 2026-09-22T19:13:23Z (UTC) | PR [#81](https://github.com/BELCORT-SDN-BHD/wringy/pull/81) (files: kickoff-package.md, m1-prototype/acceptance-record.md, PROGRESS.md, PRD.md, ARCHITECTURE.md); [#9](https://github.com/BELCORT-SDN-BHD/wringy/issues/9); `gh pr view 81 --json mergedAt,files` (2026-09-23) | None for this sub-item. The inherited M1 items stay open in their landing tickets (§1.6) |

The W2 worker builder labelled three of its rows "M2-AC01/1 (narrow loop, worker leg)". They are
evidence of the ticket's narrow loop, not of this register, so they are recorded under "Ticket
verification" below; since `c30da02` the worker's heartbeat, round-trip and drain tests carry
`M2-AC01` alone ("M2-AC01/1 is the M1 handoff register", commit message of `c30da02`).

## M2-AC01/2 — dependency intersection, fresh-migration read, account separation, no secrets

Spec: "锁定受支持依赖交集；全新数据库迁移后，页面→Fastify→PostgreSQL读取成功；迁移账户与运行账户分权，浏览器包和日志不含密钥".
The automated evidence is every test whose full name carries `M2-AC01/2`: 105 tests at `9615439`
(`pnpm check:acceptance`). The rows below consolidate the W0–W3 evidence by the four parts of the
sub-item; where a review fix later changed a result, the row says so and "W5 adversarial review"
has the fix.

### Part 1 — the supported dependency intersection is locked

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/2 part 1: Node 24 | `5ab89e1` (W0); no database | Local, Windows 11; real | `pnpm node -v`, `pnpm --filter web exec node -v`, `pnpm install --frozen-lockfile`, then the M1 web gates (lint, typecheck, test, build, e2e) under Node 24 | pnpm commands run Node 24.21.0; the frozen lockfile installs; the M1 gates pass | `v24.21.0` from both (the machine's bare `node` stays v20.19.5); "Already up to date"; lint 0 findings, typecheck ok, 322 unit tests in 24 files, build ok, e2e 318 passed, 33 skipped, 0 failed | 2026-09-23 03:27 +08:00 (commit), W0–W1 worker | `.npmrc` `use-node-version`; `.nvmrc` (CI `node-version-file`); root `package.json` `engines`; `apps/web/package.json` `@types/node` ^24 (resolved 24.13.6); [nodejs.org/dist/index.json](https://nodejs.org/dist/index.json) dates v24.21.0 to 2026-09-07 | None |
| M2-AC01/2 part 1: exact pins | `d59a007` (W1); `d60f579`, `e5a0745`, `9001493` (W2), merged in `d78ef67` | Local, Windows 11, and a fresh clone; real | Read the manifests and `pnpm-lock.yaml`; fresh clone `pnpm install --frozen-lockfile`; `npm view` for esbuild, pino and the pg-boss engines; the drift tests `packages/db/src/expected-head.test.ts` (3) and `apps/worker/src/connections.test.ts` | Exact pins resolved by the lockfile; a reproducible install; the installed pg-boss schema and the newest migration equal the build's expectations | Exact pins: embedded-postgres and every platform binary 17.10.0-beta.17, dependency-cruiser 18.4.0, node-pg-migrate 9.0.0, pg 8.23.0, zod 4.6.5, pg-boss 12.33.5, fastify 5.12.5, @fastify/type-provider-zod 1.0.0, esbuild 0.28.2 (the latest release, `npm view esbuild version`), tsx 4.23.15, pino 10.3.1, @types/pg 8.23.1. The api and worker lockfile importers resolve them, with no swagger peer; apps/worker declares no fastify and no `@wringy/contracts`. Fresh-clone frozen install exit 0 with no ignored-build warning. `EXPECTED_PGBOSS_VERSION` equals the installed pg-boss schema (42), and the pg-boss pin equals the installed 12.33.5; `npm view pg-boss@12.33.5 engines` gives node >=22.12.0 | 2026-09-23 04:03–05:30 +08:00; W1 worker, W2 builders, W2 integration | `pnpm-workspace.yaml` (overrides, `allowBuilds`, `packageExtensions`); `packages/*/package.json`; `apps/api/package.json`; `apps/worker/package.json`; `pnpm-lock.yaml`; kickoff-package.md §5.3 | None |
| M2-AC01/2 part 1: PostgreSQL 17 and Node 24 asserted | `922c1ca` (review fix test-integrity-9); head 0007 | Local, Windows 11, embedded PostgreSQL 17.10; real | `packages/db/test/cluster.int.test.ts` "M2-AC01/2 runs on PostgreSQL 17 and Node 24" and "M2-AC01/2 refuses PostgreSQL 16 and Node 20" | The integration harness refuses a Node major other than `.nvmrc`'s and a server major other than the pinned embedded-postgres major, both read from their source files | Both pass; plain Node 20.19.5 running the integration config now fails in global setup | 2026-09-23 10:45 +08:00 (commit), W5 review-fix worker | `packages/db/test/cluster.int.test.ts` L110, L120; `packages/db/test/cluster.ts` | None: ran green in the CI `integration` job on `12a85ab` and `fe17df4` (runs 35825491353, 35828034405) |

### Part 2 — after a fresh migration, the page reads through Fastify from PostgreSQL

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/2 part 2: fresh migration | `d59a007` (W1, 0001); `d60f579` (W2a, head 0005) | Local, Windows 11: a throwaway embedded PostgreSQL 17.10 per run, and the `pnpm db:start` cluster; real | `packages/db/test/migrations.int.test.ts`: "a fresh database migrates from zero as the migrator (pg-boss schema, then every SQL migration), and a second run changes nothing"; "node-pg-migrate alone stops at 0005 until pg-boss is installed, and the whole batch rolls back"; the down-then-up catalog test. Manual: `pnpm db:start`, `pnpm db:bootstrap`, `pnpm db:migrate` twice | From zero: pg-boss installed at `EXPECTED_PGBOSS_VERSION`, every SQL migration applied, the head the newest; a second run changes nothing; without pg-boss the whole batch rolls back; down then up leaves the catalog identical | Passed: `{pgboss: {from: null, to: 42, outcome: 'installed'}, migrations: [0001..0005], head: '0005_pgboss_grants'}`, then `{outcome: 'unchanged', migrations: []}`. Without pg-boss: "schema pgboss is missing", and `app`, `ops.environment` and `app.orgs` did not exist afterwards. The catalog snapshot (schemas, relations with ACLs, columns, constraints, indexes, triggers, functions with ACLs and md5, types, default ACLs) was equal after down and up; a planted leftover default ACL in 0005's Down made it fail (`arw` vs `arwd`). Manual: "Ran 1 migration(s) up: 0001_schemas_roles", then "already at the newest migration". db test:int 39 passed (7 files) at `d60f579` | 2026-09-23 04:03 and 04:43 +08:00 (commits); W1 worker, W2a db builder | `packages/db/test/migrations.int.test.ts` L91, L133, L177 (titles at `9615439`); the guard block in `packages/db/migrations/0005_pgboss_grants.sql` | The head is 0007 since the review fixes; the same tests pass there (db test:int 49 passed, 9 files, "Review-fix gate run") |
| M2-AC01/2 part 2: API leg | `e5a0745` (W2b), merged `d78ef67`, retitled in `c30da02` and `e35a273`; head 0005 | Local, Windows 11: a clone of the harness template database on embedded PostgreSQL 17 (Vitest, `app.inject`); real | `apps/api/tests/integration/internal-campaigns.int.test.ts` "M2-AC01/2 Fastify→PostgreSQL read (API leg): GET /internal/campaigns returns the seeded fixture campaigns" and the two allow-list tests; `health.int.test.ts` (ok at the head; behind the head); `worker-health.int.test.ts` (process state and `queueState`) | 200 with the three seeded fixtures and no live row; `dataAsOf` from the database clock; `Cache-Control: private, no-store`; `/health` 200 at the head and 503 behind it; worker states from the database clock; only the contract's keys in a response | All passed. Campaigns: 3 fixtures, the live org and campaign absent. `/health`: 200 `{migrationHead: 0005_pgboss_grants, queueSchemaVersion: 42}`; a build expecting 0006 got 503 with `migrations` failing; after the real 0005 down migration both reads failed with 42501, in the log only, never in the body. Worker health: healthy, stale, stopped, never_seen and `queueState` ok, overdue, never, unchanged with the host clock moved (a mutation using `new Date()` for `dbNow` failed it). Allow-list: with an `internal_note` column holding a canary, and a probe handler passing every column plus `debugSql` and `connectionString`, only the 6 contract keys came back. api test:int 24 passed + 1 skipped (W2), 25 + 1 (W3, with `queueState`) | 2026-09-23 05:16–07:17 +08:00; W2 api builder, W2 integration, W3 build | `apps/api/tests/integration/{internal-campaigns,health,worker-health}.int.test.ts`; `apps/api/src/worker-state.test.ts` (45 s + 1 ms boundary); [type-provider-zod v1.0.0 `src/core.ts`](https://raw.githubusercontent.com/fastify/fastify-type-provider-zod/v1.0.0/src/core.ts) (`safeEncode` serializer) | Since `18099af` the API reads the pg-boss version through `ops.pgboss_schema_version`, not `pgboss.version` (review finding security-privileges-4) |
| M2-AC01/2 part 2: page leg, cold start | `fb46d3c` (W3), re-run on `894d8a1` (W3 fix) and in CI on `9ca00a5`; head 0005 | Local, Windows 11: `next start`, apps/api, apps/worker and a PostgreSQL 17 database migrated from zero, seeded and marked `ci` (embedded). CI: `ubuntu-24.04`, `postgres:17` service. Real, nothing mocked | `pnpm e2e:internal`: `apps/web/tests/e2e-internal/internal.spec.ts` "M2-AC01/2 cold start: /internal lists the three seeded fixture campaigns read through Fastify from PostgreSQL (<locale>)" for en-MY, ms-MY and zh-Hans-MY in the mobile, desktop and small projects | `/internal` 200 in each locale with `<html lang>`; three rows with title, org, a localized status, the fixture badge and an update time in `(UTC+08:00)` whose `datetime` equals `app.campaigns.updated_at` read as the migrator; no failure state | All 9 runs passed. The suite: 28 passed, 4 skipped, three runs in a row on `fb46d3c` (44.8 s, 45.7 s, 46.5 s) and three on `894d8a1` (45.6 s, 43.0 s, 44.2 s; 0 capture errors, 0 shutdown failures); CI e2e job on `9ca00a5`: 28 passed, 4 skipped (50.4 s). The 4 skips are viewport scoping | 2026-09-23 07:17 and 07:58 +08:00 (W3 build, W3 fix); CI 2026-09-23T02:04:22Z | `docs/m2-internal/screenshots/cold-start-{390,1440,320}.png` (`7136eff`); [CI e2e job](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107012270445) | None: also green in the CI `e2e` job on `12a85ab` and `fe17df4` (runs 35825491353, 35828034405) |
| M2-AC01/2 part 2: worker health on the page | `fb46d3c` (W3); CI `9ca00a5`; head 0005 | As the row above; real | `internal.spec.ts` "M2-AC01/2 worker health: the running worker reports healthy within 30 s and the queue round trip becomes ok or is shown as unknown, never 0" | The `e2e-worker-1` card reads "Process: Healthy" within 30 s with image `local/e2e`; the queue reads "Round trip OK" with a time, or "Unknown"; no field reads 0 or a dash | Passed at 390, 1440 and 320, locally and in CI. The 1440 frame shows `e2e-worker-1` "Process: Healthy" and "Queue: Round trip OK" (looked at 2026-09-23 by the W5 record worker) | 2026-09-23 07:17 +08:00, W3 build | `docs/m2-internal/screenshots/worker-health-{390,1440,320}.png`; `internal.spec.ts` L98 | None |

### Part 3 — the migration account and the runtime accounts are separated

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/2 part 3: roles | `d59a007` (W1, 0001) | Local, Windows 11: embedded PostgreSQL 17.10, and again with `TEST_DATABASE_URL` at the `pnpm db:start` cluster; real | `packages/db/test/roles.int.test.ts`, `migrations.int.test.ts`, `pool.int.test.ts`; manual `pnpm db:bootstrap` (without `WRINGY_ENV`; `staging` without secrets; twice with `local`) and a probe as `wringy_api_login` | The migrator owns `app` and `ops`; nothing in `public`; the API login's CREATE refused (42501); the worker login has no privilege on `app`; default privileges as designed; logins not elevated; no login without CONNECT (42501); the advisory lock refuses a concurrent run; bootstrap refuses outside local without an admin URL and passwords, and is idempotent | 13 passed on both clusters. Manual: "WRINGY_ENV is missing", exit 1; the four `PG_BOOTSTRAP_*` names reported missing; "database: wringy (created)", then "(existed)"; the API login's CREATE in `app` → 42501 "permission denied for schema app"; TimeZone UTC | 2026-09-23 04:03 +08:00 (commit), W1 worker | `packages/db/test/roles.int.test.ts`; `packages/db/migrations/0001_schemas_roles.sql`; `packages/db/scripts/bootstrap.mjs`; `packages/db/src/bootstrap.ts` | The local-password fallback was narrowed to the embedded cluster by `a65a342` (review findings security-privileges-2, correctness-runtime-8) |
| M2-AC01/2 part 3: grant manifest and refusals | `d60f579` (W2a, head 0005) | Local, Windows 11, embedded PostgreSQL 17.10; real | `packages/db/test/grants.int.test.ts` "M2-AC01/2 runtime role privileges match reviewed grant manifest" (`has_schema/table/sequence/function_privilege` for both logins against `grant-manifest.ts`), the ACL-grantee test and the three 42501 tests | Effective privileges equal the manifest exactly; only the migrator, `wringy_api` and `wringy_worker` in any ACL; no PUBLIC EXECUTE on a pg-boss function; the worker cannot read `app.campaigns` or `app.orgs`; the API cannot write `ops.worker_heartbeat` nor read `pgboss.job`; neither can change the marker | Passed (db test:int 39 of 39). Mutation: with 0002's REVOKE for the worker dropped, the manifest test failed (the worker had INSERT, UPDATE on `ops.environment`); the files were restored byte-identical | 2026-09-23 04:43 +08:00 (commit), W2a db builder | `packages/db/test/grants.int.test.ts`; `packages/db/test/grant-manifest.ts` (the API: SELECT only, L55) | Widened to column grants and every schema by `651bc2c`; the API lost all `pgboss` access in `18099af` |
| M2-AC01/2 part 3: runtime logins at work | `e5a0745`, `9001493`, merged `d78ef67`; head 0005 | Local, Windows 11: embedded clones and the `pnpm db:start` cluster; real | `apps/api/tests/integration/runtime-role.int.test.ts`; `apps/worker/test/roles.int.test.ts` "M2-AC01/2 the worker runs as the runtime role and cannot read app.campaigns" and "M2-AC01/2 pg-boss runs under the runtime role with DML only" (ruling D33: `createQueue` as the runtime role); the merged worker started live | API: `current_user` `wringy_api_login`, `application_name` `wringy-api`, a member of `wringy_api` only; INSERT, UPDATE, DELETE, TRUNCATE on `app.campaigns`, INSERT on `app.orgs`, the heartbeat and the marker → 42501. Worker: both pools (`wringy-worker`, `wringy-worker-pgboss`) log in as `wringy_worker_login`; `app` and the marker → 42501; no CREATE on `pgboss`, `ops` or the database; queue `system.heartbeat` with `partition=false`, `table_name=job_common`, schedule `* * * * *` | All passed; the worker roles test again in 3 more runs. On the merged tree the worker logged "worker started", and the cron job completed a round trip as the runtime login with no 42501 and no extra migration | 2026-09-23 05:16–05:30 +08:00; W2 api and worker builders, W2 integration | the two test files; `node_modules/pg-boss/dist/plans.js` `create_queue` (INSERT … ON CONFLICT DO NOTHING) | The worker's rights on `pgboss.version` and `pgboss.queue` were bounded by `0006` (review finding security-privileges-1) |
| M2-AC01/2 part 3: environment and fixture isolation | `d60f579`, `e5a0745`, `9001493`; head 0005 | Local, Windows 11; real | `packages/db/test/fixture-guard.int.test.ts`; `seed.int.test.ts` plus `pnpm db:seed:fixtures` twice and once before `pnpm db:env`; `startup.int.test.ts` of the api and of the worker (environment mismatch, no marker); the worker's start with the pg-boss schema missing or behind | Fixture rows refused (23514, `ops_environment_fixtures_allowed`) where not allowed and without a marker; a campaign whose origin differs from its org refused (23503); the seed writes 5 rows (2 orgs, 3 campaigns), then 0, and is refused without a marker, under production or on a `WRINGY_ENV` mismatch; both processes refuse a mismatch, naming both environments and no secret | All passed. Manual: "5 row(s) inserted or changed", then "0 row(s) inserted or changed"; before `db:env`: "Fixture seed refused: This database has no ops.environment marker; run pnpm db:env first.", exit 1. API: `EnvironmentRefusedError` in process, and exit 1 as a child. Worker: `environment_mismatch` and `environment_unmarked` with no heartbeat row and 0 pg-boss sessions; the pg-boss schema renamed away gave "pg-boss is not installed", versions 41 and 43 gave "pg-boss database requires migrations", and it started once 42 was restored | 2026-09-23 04:43–05:30 +08:00; W2 builders | `packages/db/migrations/0002_environment_marker.sql`, `0003_orgs_campaigns.sql`; the four test files | Relabelling a fixture-holding database to production, and relabelling a fixture row live, were closed by `37a7c2d` and `b2c1fb5` (review findings correctness-runtime-6, -7) |

### Part 4 — no secret in the browser bundle or the logs

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/2 part 4: configuration errors and examples | `d59a007` (W1); `c30da02` (W2 fix) | Local, Windows 11; real | `pnpm --filter @wringy/config test` ("an env error never echoes a value"); the SCRAM verifier tests; `apps/api/src/config.test.ts` and `apps/worker/src/config.test.ts`, with a planted `EXTRA_NAME=` line in `apps/worker/.env.example` | Env errors name variables only; role passwords never appear in SQL text; each app's `.env.example` names exactly its schema's variables, with no value | 15 config tests passed, with value-leak checks across message, stack, JSON and every schema; 8 db unit tests with the SCRAM verifiers; the example tests pass, and the planted line failed ".env.example names exactly the variables the worker reads, and no value" (+ "EXTRA_NAME"), then was removed | 2026-09-23 04:03 and 06:03 +08:00 (commits); W1 worker, W2 fix | `packages/config/src/env.test.ts`; `packages/db/src/scram.ts`, `scram.test.ts`; the two `config.test.ts` | None |
| M2-AC01/2 part 4: process output | `d60f579`, `e5a0745`, `9001493`, `d78ef67`, `c30da02`; head 0005 | Local, Windows 11; real (pino output captured, child processes) | db: the first migrations test asserts no migrator password in the output, and the pg-boss CLI gets its URL through the environment; api `secrets.int.test.ts` "M2-AC01/2 no secret leaves the process: /health and error bodies never contain the connection string, and logs redact DATABASE_URL" (10 requests: 200, 404, 500, 503); worker `process.int.test.ts` "M2-AC01/2 no secret in logs"; the logs of the two live loops | No body or log line holds either connection string, either password, `postgres://` or the token; no stack in a body; `databaseUrl`, `password`, `apiToken` keys become `[Redacted]`; the 500 log keeps its stack with the URL scrubbed | All passed. A mutation that disabled the api's `formatters.log` failed the test ("log leaks postgres://w…"). Worker: every line JSON, exit codes 0, 1, 1, 1, `stopped_at` set after the graceful run. The live-loop logs (api, two workers): 0 matches for `postgres://` and for the committed local-development password prefix | 2026-09-23 04:43–06:03 +08:00; W2 builders, W2 integration, W2 fix | `apps/api/src/logger.ts`; `apps/worker/src/logger.ts`; `packages/db/src/pgboss.ts` `runCli()`; the test files | None |
| M2-AC01/2 part 4: secret canary | `5560a4e` and `fb46d3c` (W3), `894d8a1` (W3 fix); CI `9ca00a5`; head 0005 | Local, Windows 11; CI `ubuntu-24.04`; real | `pnpm canary` (`scripts/check-secret-canary.mjs`): builds web, api and worker with a canary in each of the 14 schema variables (plus `TEST_DATABASE_URL`), scans every output, runs the built api and worker for 10 s against an unreachable database whose URL carries the canary password; a self-test plants a canary first | 0 canary values in any bundle, build output or log; the self-test finds its plant | PASS locally on `fb46d3c`: 0 in `.next/static` (72 files), `.next/server` (756), the rest of `.next` (36), api `dist` (2), worker `dist` (2); api log 5 lines and worker log 5 lines, 0; self-test 1 hit. PASS in the CI check job on `9ca00a5`: 72, 756, 1731, 2 and 2 files, 0 each; api log 5 lines, worker log 9 lines, 0 | 2026-09-23 07:04–07:58 +08:00 (W3); CI 2026-09-23T01:47:41Z | [CI check job](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107011863199) ("canary: PASS: no canary value in the browser bundle, the server build, the api and worker bundles, or their logs") | None |

## Ticket verification: cold start, health page, a rejected dependency, recovery

`m2-01.md` "Verification and recovery": 展示冷启动、健康页及一次依赖违规被检查拒绝；移除新入口可回到原型，保留源设计资产;
涉及…任务状态用真实PostgreSQL集成测试；涉及用户操作给端到端证据，包含至少一个实质失败/越权场景. Where each is shown:

| Verification | Evidence |
|---|---|
| 冷启动 (cold start) | "M2-AC01/2 part 2: page leg, cold start" above |
| 健康页 (health page) | "M2-AC01 narrow loop, live" below; `/health` in "M2-AC01/2 part 2: API leg" |
| 一次依赖违规被检查拒绝 (a rejected dependency) | "M2-AC01/3 dependency direction" |
| 移除新入口可回到原型，保留源设计资产 (recovery) | "Recovery: removing the new entry returns the prototype" |
| Task state on real PostgreSQL | "M2-AC01 worker heartbeat and queue round trip" and "M2-AC01 worker drain" below |
| An end-to-end failure or refusal (kickoff-package.md §6.7: a wrong import rejected, a stopped worker stale, a stopped API an error state) | "M2-AC01 failure and refusal states" below; "M2-AC01/3 dependency direction" |

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01 narrow loop, live | `d78ef67` (W2 integration), `c30da02` (W2 fix); head 0005 | Local, Windows 11: the `pnpm db:start` cluster (127.0.0.1:54329); the api and worker as real processes from `apps/*/.env`; real | After `pnpm db:migrate`, `db:env`, `db:seed:fixtures`: start the API and a worker (in the second run workers A and B) and poll `/health`, `/internal/campaigns` and `/internal/worker-health` with curl; stop a worker through its IPC `shutdown`, kill another, then stop the API | A healthy row within 20 s; the round trip stamped; `stopped` after a graceful stop and `stale` after a kill; a clean failure once the API stops; 3 fixture campaigns; `/health` ok; no secret in any log | Run 1: healthy about 6 s after the start (first beat 21:22:22.350Z, API healthy 21:22:27Z), round trip 21:22:28.396Z; IPC `shutdown` exited 0 and the API showed `stopped` (`stopped_at` 21:23:41.639Z ≥ `last_beat_at` 21:23:41.444Z); curl exit 7 once the API stopped; the same loop passed on the merged dist bundles. Run 2: healthy after 0.5 s; `/health` 200 `{ok, 0005_pgboss_grants, 42}`, private, no-store; round trip 21:57:33.215Z after the start at 21:57:27.187Z; `stopped` after IPC; `stale` after the kill (dbNow 21:58:19.140Z); ECONNREFUSED once the API stopped. 0 secret matches in the api and worker logs | 2026-09-22T21:22Z–21:58Z (UTC; 05:22–05:58 +08:00 on 2026-09-23); W2 integration, W2 fix | `apps/api/src/worker-state.ts`; `apps/worker/src/shutdown.ts` | On Windows `process.kill('SIGTERM')` is a forced kill (the row went `stale`, no stop recorded); the real signal is proven on Linux (the drain row below) |
| M2-AC01 worker heartbeat and queue round trip | `9001493` (W2c), merged `d78ef67`; `e3d71ae` and `27de11a` (review fixes) | Local, Windows 11: embedded PostgreSQL 17 clones, and the `pnpm db:start` cluster for the built worker; real | `apps/worker/test/heartbeat.int.test.ts` "worker heartbeat row appears within one beat and uses the database clock" and "queue round trip: a sent system.heartbeat job updates last_queue_round_trip_at"; `apps/worker/test/schedule.int.test.ts` "M2-AC01/2 the pg-boss schedule becomes a queue round trip with no job sent by hand" (review finding test-integrity-6); `node dist/main.js` for 65 s as `wringy_worker_login`, then IPC `shutdown` | The beat uses the database clock (a process `Date` faked to 2001 changes nothing); the round trip is stamped with a database time; the schedule alone produces a round trip | Passed (2500 ms, 1211 ms). The scheduled round trip passed locally in 6.8 s (it waits up to 120 s), with no probe job, the schedule row present and `cron_on` stamped. The 65 s run: exit 0; started 21:16:38.035Z, last beat 21:17:38.105Z, round trip 21:17:08.155Z (cron-driven), stopped 21:17:42.855Z; an earlier 75 s run of the build had set `pgboss.queue` `maintain_on` and `monitor_on`, so pg-boss maintenance ran as the runtime role | 2026-09-23 05:18 and 11:01 +08:00 (commits); W2 worker builder, W5 review-fix worker | the two test files; `apps/worker/README.md` "Tests" | None: `schedule.int.test.ts` ran green in the CI `integration` job on `12a85ab` and `fe17df4` (runs 35825491353, 35828034405) |
| M2-AC01 worker drain (rehearses M2-AC09/3) | `9001493` (W2c); `27de11a` (review fix correctness-runtime-3); CI `9ca00a5` | Local, Windows 11, embedded; CI `ubuntu-24.04` with `postgres:17`; real | `heartbeat.int.test.ts` "graceful stop drains and sets stopped_at" and "M2-AC01 graceful stop starts the pg-boss drain at once while a beat is stuck on a lock, then marks stopped_at"; `apps/worker/test/process.int.test.ts` (IPC `shutdown` on win32, `SIGTERM` on Linux, L60–L61); apps/api `startup.int.test.ts` "SIGTERM closes the server and the pool, and the process exits 0" (skipped on win32, L133); a `child.kill('SIGTERM')` probe of the built worker on Windows | A running job completes rather than fails; `stopped_at` ≥ the last beat and the last round trip; no pg-boss session left; a real SIGTERM drains | Local: passed (2833 ms); with `graceful: false` swapped in it failed ("expected 'retry' to be 'completed'"). After `27de11a` pg-boss stops within 3 s while a beat is stuck. The Windows probe: abrupt termination, no shutdown log, `stopped_at` NULL, which is why the IPC path exists. CI integration on `9ca00a5`: apps/api 26 passed with none skipped (the SIGTERM test ran) and apps/worker 12 passed (`process.int.test.ts` sends SIGTERM there) | 2026-09-23 05:18 +08:00 (W2c); CI 2026-09-23T01:46:44Z | [CI integration job](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107011863435) | M2-AC09/3 itself is M2-09's (staging); a SIGTERM inside a container, and Render's grace period, are unverified |
| M2-AC01 failure and refusal states | `fb46d3c` (W3) and `9f57a6b`; CI `9ca00a5`; head 0005 | Local, Windows 11; CI `ubuntu-24.04`; real stack, nothing mocked | `internal.spec.ts` "M2-AC01 a worker whose beat is overdue shows stale" and "M2-AC01 a worker that recorded a clean stop shows stopped" (rows written as `wringy_worker_login`); `outage.spec.ts` "M2-AC01 the API being unreachable shows the api-unreachable state and no stack or URL"; `database-outage.spec.ts` "M2-AC01 the database being unavailable to the API shows the api-unavailable state, never an empty list" (the `wringy_api` group's CONNECT revoked, then granted back); the two not-found tests | Stale/overdue and stopped/unknown in their localized words; `api-unreachable` shown once, in 3 locales, with no port, ECONNREFUSED or stack on the page; `api-unavailable` after a 503, never an empty list, and the page recovers once CONNECT is back; a 404 inside the right root layout | All passed on `fb46d3c` and in CI on `9ca00a5`. The web server logged one line per failed read and nothing more: "[internal] GET /internal/campaigns failed: api-unreachable (ECONNREFUSED)" and "… api-unavailable (HTTP 503)" (CI e2e log). 404: before `9f57a6b` a bare `<html>` with 0 stylesheets; after it the browser shows `lang`, the CSS, the banner and the internal not-found page, or the demo layout | 2026-09-23 06:49–07:17 +08:00 (commits), W3 build; CI 2026-09-23T02:04Z | `docs/m2-internal/screenshots/worker-stale-1440.png`, `api-unreachable-{390,1440}.png`, `api-unavailable-1440.png`, `internal-not-found-{390,1440}.png`; [CI e2e job](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107012270445) | The raw HTML a `curl` receives for a 404 is still Next's `__next_error__` shell ([known-issues.md](known-issues.md)) |
| M2-AC01 the internal entry stands apart from the demo | `5de916d` (W1) | Local, Windows 11; real | Web lint, typecheck, test, build and the full e2e, including "M2-AC01 internal build route walk › /internal renders in <locale> without console errors and without the demo store" (renamed in `c30da02`); a mutation that mounted `AppProviders` in `(internal)` | M1 URLs unchanged and the suite green; `/internal` in en-MY, ms-MY and zh-Hans-MY with the `lang` cookie, the trilingual banner, no console error, no demo toolbar or prompt, and no `wringy-demo-v1` in localStorage | e2e 327 passed (318 M1 + 9 new), 33 skipped, 0 failed; the mutation failed all 3 locale tests (demo key present) and was reverted | 2026-09-23 04:03 +08:00 (commit), W0–W1 worker | `apps/web/src/app/(internal)/layout.tsx`; `apps/web/tests/e2e/i18n.spec.ts`; `apps/web/src/messages/*/internal.json` | The removal itself: "Recovery" below |
| M2-AC01 test names carry the acceptance ID | `c30da02` (W2 fix), `894d8a1` (W3 fix), `f16c95e` (review fix) | Local, Windows 11; real | `vitest list --json` over api, worker, db (unit and int), config and contracts, before and after the retag; a grep over the W3 test files; `pnpm check:acceptance` (a) | Every M2-01 test's full name carries `M2-ACxx` (kickoff-package.md §6.1; `m2-01.md` "本路径测试命名含 M2-AC01") | Before `c30da02`: 137 of 181 untagged (api 36+19, worker 23+10, db 23+2, config 15, contracts 9); after: 0 of 181. W3: 0 unmarked titles across the 11 changed files (77 earlier titles marked through their describe). At `9615439`: "(a) OK: all 250 names carry M2-AC" | 2026-09-23 06:03, 07:58 and 10:56 +08:00 (commits) | `scripts/check-acceptance-mapping.mjs` | None |
| M2-AC01 evidence capture survives Chromium's empty frame | `894d8a1` (W3 fix) | Local, Windows 11; real Chromium | `apps/web/tests/unit/capture-retry.test.ts` (4 tests); a fault injected into a real Chromium page | One transient "Unable to capture screenshot" → a second capture writes the frame; 3 transient failures → rejected after 3 calls; any other error → rejected after 1 | 4 passed (web total 334); the injection gave 2 calls and a 7048-byte frame, rejection after 3 calls, and rejection after 1 call | 2026-09-23 07:58 +08:00 (commit), W3 fix | `apps/web/tests/e2e/capture-retry.ts`; Chromium `page_handler.cc` L1983–L1987 (an empty image → "Unable to capture screenshot") | The flake itself: [known-issues.md](known-issues.md) |

## M2-AC01/3 — workspace and CI checks; branch protection, cross-vendor review, release gate

Spec: "建立独立工作区和CI的类型、单元、依赖方向、spec引用及验收映射检查；核验分支保护、跨厂商审阅和发布门禁，未能配置项报告成本及处置，不伪称已生效".

### Workspace and CI checks

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/3 workspace | `feat/m2-01` (W4) | Local, Windows 11; real | `pnpm install --frozen-lockfile` over `pnpm-workspace.yaml` (`apps/*`, `packages/*`): apps web, api, worker; packages config, contracts, db | One workspace, one lockfile, each package declaring only what it imports | `pnpm install` and every gate below ran on this workspace (W4 gate run); the api and worker builds fail on any undeclared external, and a local `pnpm deploy --legacy` of each app installed only its production dependencies | 2026-09-23, W4 worker | `pnpm-workspace.yaml`; `pnpm-lock.yaml` | None |
| M2-AC01/3 local gates, W0–W3 | `5ab89e1` … `894d8a1` (per wave, in "W0–W3 gate runs" below) | Local, Windows 11; real | The root gates at every wave: `pnpm lint`, `pnpm typecheck` (类型), `pnpm test` (单元), `pnpm test:int`, `pnpm build`, `pnpm depcruise` (依赖方向), `python -X utf8 scripts/check-planning.py` (spec引用), `git diff --check`, and the M1 suite | Each exits 0 at every wave; the M1 suite stays green | Exit 0 at every wave; counts per wave in "W0–W3 gate runs" | 2026-09-23 03:27–07:58 +08:00 (commits); the W0–W3 workers | "W0–W3 gate runs" | None |
| M2-AC01/3 dependency direction | `d59a007` (W1) to `894d8a1` (W3 fix); `bc1f7fd` (review fix); CI `9ca00a5` | Local, Windows 11; CI `ubuntu-24.04`; real | `pnpm depcruise` (`scripts/check-dependency-direction.mjs`, `.dependency-cruiser.cjs`) with its planted violation; temporary probes, each deleted afterwards: an apps/api file importing apps/web, an apps/worker file importing fastify, apps/web and `@wringy/contracts`, and manual probes of the W1 rules | The tree passes every kickoff-package.md §8.7 rule plus no-circular; each planted import is rejected by its rule | W1: "no dependency violations found (303 modules, 1235 dependencies cruised)"; "planted violation rejected (exit 2): error web-not-to-server-runtime: apps/web/src/lib/__dependency-violation__.ts → @wringy/db"; manual probes also fired `internal-not-to-demo`, `web-ui-not-to-server-config` and `contracts-config-not-to-runtimes`. W2: the api probe was rejected by `api-not-to-other-apps`; the worker probe gave 4 errors (`worker-not-to-other-apps-or-fastify` ×2, `not-to-unresolvable` ×2), and no direction rule named worker → `@wringy/contracts` until `bc1f7fd`. W3: 359 modules, 1397 dependencies, 0 violations, the planted file rejected (exit 1). CI check job on `9ca00a5`: 359 modules, 1399 dependencies, planted violation rejected, "depcruise: PASS". After `bc1f7fd`: 362 modules, 1409 dependencies, two planted violations rejected in one cruise, each by its own rule (`web-not-to-server-runtime`, `worker-not-to-contracts`) | 2026-09-23 04:03 +08:00 to 11:02 +08:00 (commits); CI 2026-09-23T01:46:53Z | `.dependency-cruiser.cjs`; `scripts/fixtures/dependency-violation.ts`, `dependency-violation-worker.ts`; [CI check job](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107011863199) | None: the two-fixture check ran green in the CI `check` job on `12a85ab` and `fe17df4` (runs 35825491353, 35828034405) |
| M2-AC01/3 a failing integration test fails the command | `e5a0745` (api guard), `d78ef67` (shared guard) | Local, Windows 11, embedded clusters; real | A planted failing probe in each of packages/db, apps/worker and apps/api, deleted afterwards; `pnpm test:int` | A failing integration test makes the command exit non-zero | Before the guard: packages/db and apps/worker exited 0 with "Test Files 1 failed", and so did apps/api before its own guard; the cause: embedded-postgres registers `async-exit-hook`, whose `beforeExit` handler calls `process.exit(0)` after Vitest has set exit code 1. After `packages/db/test/exit-code-guard.ts` was installed from `global-setup.ts` (it replaced the api's own guard): db, worker and api each exit 1, db also on an embedded cluster | 2026-09-23 05:16 and 05:30 +08:00 (commits); W2 api builder, W2 integration | `packages/db/test/exit-code-guard.ts` L4–L13; `node_modules/.pnpm/async-exit-hook@2.0.1/node_modules/async-exit-hook/index.js` (`add.hookEvent('beforeExit', 0)`) | None |
| M2-AC01/3 CI checks | `feat/m2-01` (W4) | Local, Windows 11; real. CI: executed on `9ca00a5` ("CI run on GitHub" below) | The CI `check` job's commands run locally: `pnpm lint`, `pnpm typecheck` (类型), `pnpm test` (单元), `pnpm depcruise` (依赖方向, including a planted violation that must be rejected), `pnpm check:acceptance` (验收映射), `pnpm build`, `pnpm canary`; `python -X utf8 scripts/check-planning.py` (spec引用, CI job `planning`) | Each check passes on the tree and fails on a planted fault | Passed locally (W4 gate run below); the dependency check rejects its planted violation on every run. The acceptance-mapping check was also shown to fail: with the `M2-AC01` tag removed from one describe block it listed the 5 untagged names and exited 1; with the tag removed from a static-parsed integration test and a Playwright test it listed both and reported M2-AC01/3 unmapped | 2026-09-23, W4 worker | `.github/workflows/app.yml` (jobs `check`, `integration`, `e2e`, `images`); `.github/workflows/planning.yml`; `scripts/check-acceptance-mapping.mjs`; `scripts/check-dependency-direction.mjs` | None: executed on `12a85ab` and `fe17df4`, see "CI on the review-fix heads" |
| M2-AC01/3 CI-only checks | `feat/m2-01` (W4); executed on `9ca00a5` | CI (`ubuntu-24.04`); real | `integration`: `pnpm test:int` against a `postgres:17` service; `e2e`: the M1 suite and `pnpm e2e:internal` on that service; `images`: the three Dockerfiles built with `push: false` and smoke-run | Green jobs on the first push | Executed on the first push, `9ca00a5`: `integration`, `e2e` and `images` **pass** ("CI run on GitHub" below). They need Docker and a Linux runner, which this machine lacks. Before the push, locally, the shared-cluster shape of `integration` was run against the `pnpm db:start` cluster (`TEST_DATABASE_URL` set; three runs, exit 0), and the images' contents were run from local `pnpm deploy` outputs on Node 24.21.0 | 2026-09-23, W4 worker (local parts) | `packages/db/README.md` "One cluster, several runs"; commit "Add the web, api and worker images and the bundled migrate step" | None for `9ca00a5` (the api SIGTERM test, skipped on win32, ran there); the review-fix heads `12a85ab` and `fe17df4` ran green too (runs 35825491353, 35828034405) |
| M2-AC01/3 CI checks (review fixes) | `feat/m2-01` after the review fixes | Local, Windows 11; real | `pnpm depcruise` and `pnpm check:acceptance` as they now run (review findings test-integrity-1 and test-integrity-3, and the worker rule from kickoff-package.md §8.1/§8.7) | Each check fails on its own planted fault, on every run, without a hand edit | `pnpm depcruise` plants two violations in one cruise and requires each rule to name its file: apps/web importing `@wringy/db` (`web-not-to-server-runtime`) and apps/worker importing `@wringy/contracts` (`worker-not-to-contracts`, new). `pnpm check:acceptance` first runs a self-test on `scripts/fixtures/acceptance-self-test/` and exits 1 unless it catches an untagged name, a declared `test.skip`, a `test.fixme`, M2-AC99/10 not counting for /1 and two run-time skips (with `names()` broken to always match, it stopped with "self-test: ... could pass what it must fail"); a Playwright spec whose every test is expected skipped no longer counts, and (c) refuses an unconditional run-time skip in a listed file. Replayed: a planted `describe.skip` + `test.fixme` spec tagged M2-AC02/1 and a `ctx.skip()` test tagged M2-AC02/2 made `node scripts/check-acceptance-mapping.mjs m2-02` report M2-AC02/1 tests 0 and (c) FAIL, exit 1 (before the fix both counted). The one test that used to carry M2-AC01/3 tested the harness's role bootstrap; it is now tagged M2-AC01, so /3 rests on these rows | 2026-09-23, review-fix worker | `scripts/check-dependency-direction.mjs`; `scripts/check-acceptance-mapping.mjs`; `scripts/fixtures/`; commits `f16c95e`, `bc1f7fd` | The CI run of these commits |

### CI run on GitHub

`gh pr checks 82` and `gh run list --branch feat/m2-01 --limit 5` on 2026-09-23 (05:48Z): two runs,
both on `9ca00a5`, both `completed success`. The jobs ran on the `pull_request` event, that is on
GitHub's merge commit of `9ca00a5` into `main` (`a6d7416`, the images job's `SHA` line). Two later
heads ran the same jobs green: `12a85ab` (runs 35825491353 and 35825491377) and `fe17df4` (runs
35828034405 and 35828034315); see "CI on the review-fix heads".

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/3 CI job check | `9ca00a5` (merge ref `a6d7416`); head 0005 | CI `ubuntu-24.04`; real | `app.yml` job `check`: `pnpm install --frozen-lockfile`, lint, typecheck, `pnpm test`, `pnpm depcruise`, `pnpm check:acceptance`, `pnpm build`, `pnpm canary` | Pass | **pass** (1m55s). Unit: contracts 9, config 15, db 25, api 42, worker 25, web 334 (26 files). depcruise PASS with the planted violation rejected; check:acceptance PASS (214 names from 10 sources); canary PASS | 2026-09-23T01:45:50Z–01:47:45Z | [job 107011863199](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107011863199) | The review-fix head (row "CI on the review-fix head") |
| M2-AC01/3 CI job integration | `9ca00a5` (merge ref `a6d7416`); head 0005 | CI `ubuntu-24.04` with a `postgres:17` service; real | `app.yml` job `integration`: `pnpm test:int` with `TEST_DATABASE_URL` at the service | Pass, with the Linux-only tests running | **pass** (1m2s). db 40 passed (8 files); api 26 passed (7 files), none skipped, so the SIGTERM test ran; worker 12 passed (4 files) | 2026-09-23T01:45:50Z–01:46:52Z | [job 107011863435](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107011863435) | As above |
| M2-AC01/3 CI job e2e | `9ca00a5` (merge ref `a6d7416`); head 0005 | CI `ubuntu-24.04` with a `postgres:17` service; real | `app.yml` job `e2e`: the M1 Playwright suite, then `pnpm e2e:internal` | Pass | **pass** (16m39s). M1 suite: 327 passed, 33 skipped (14.6m). Internal suite: 28 passed, 4 skipped (50.4 s) | 2026-09-23T01:47:49Z–02:04:28Z | [job 107012270445](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107012270445) | As above; `failOnFlakyTests` (`ddfd33e`) was not yet in this run |
| M2-AC01/3 CI job images | `9ca00a5` (merge ref `a6d7416`) | CI `ubuntu-24.04`, Docker Buildx; real | `app.yml` job `images` (`app.yml` L158–L223): build the web, api and worker images with `push: false`, then smoke-run them | Each image builds; api, migrate and worker stop at their environment check with names only; all three run as `node`; the web image serves `/internal` | **pass** (1m57s). "Invalid environment for api: DATABASE_URL is missing; WRINGY_ENV is missing. Values are not shown.", the same for migrate and for the worker (JSON, names only); `/internal` rendered `data-app-state="not-configured"`; "images: all three started as node and reached their checks". The web container's first probe inside the wait loop got "curl: (56) Recv failure: Connection reset by peer" before the server listened | 2026-09-23T01:47:48Z–01:49:45Z | [job 107012270455](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628772/job/107012270455) | Images are proven only here (no Docker locally); pushing and pulling them is M2-09 |
| M2-AC01/3 CI job planning | `9ca00a5` (merge ref) | CI; real | `planning.yml` job `planning`: `scripts/check-planning.py` | Pass | **pass** (12s) | 2026-09-23T01:45:47Z | [Planning checks run 35807628875](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35807628875) | As above |
| M2-AC01/3 CI on the review-fix heads | `12a85ab` (`18099af` … `9615439` plus the record commit) and `fe17df4` (`2e84a7a`, `fe17df4`); head 0007 | CI `ubuntu-24.04` with a `postgres:17` service; real | `app.yml` jobs `check`, `integration`, `e2e`, `images` and `planning.yml` on each head | Green on both heads, including the tests added by the review fixes (timeouts, scheduled round trip, harness guards, PostgreSQL 17 / Node 24 assertions, `failOnFlakyTests` active in the internal suite, the relabel-serialisation tests, the request-log tests) | **pass** on `12a85ab`: check 1m55s, integration 1m24s, e2e 13m1s, images 2m11s, planning 11s. **pass** on `fe17df4`: all four `app.yml` jobs `success` and planning `success` | 2026-09-23 (runs recorded by the orchestrator after each push) | [run 35825491353](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35825491353), [run 35825491377](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35825491377); [run 35828034405](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35828034405), [run 35828034315](https://github.com/BELCORT-SDN-BHD/wringy/actions/runs/35828034315) | None. The re-record commit that writes this row is documentation only; its run is cited on PR #82 and #16 |

### Governance: branch protection, cross-vendor review, release gate

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01/3 branch protection (today) | `main` settings on GitHub | GitHub repository settings; real | Read the protection of `main`: `gh api repos/BELCORT-SDN-BHD/wringy/branches/main/protection` | Report what is enforced, without claiming more | Required status checks: `["planning"]` only (app id 15368), `strict: true`; a pull request is required with 0 approving reviews and stale reviews dismissed; `enforce_admins` on; linear history and conversation resolution required; force pushes and deletion off; no rulesets. The product checks (`check`, `integration`, `e2e`) are **not** required today, so a red product build could merge | 2026-09-23: orchestrator check (kickoff-package.md §2.1); re-read by the W4 worker at 2026-09-23T00:46:55Z and by the W5 record worker at 2026-09-23T05:48:15Z (required checks `["planning"]`, strict, 0 approvals, `enforce_admins` on) with the same result | [kickoff-package.md](kickoff-package.md) §2.1; `gh api …/branches/main/protection`, `gh api …/rulesets` (0) | D22 applies after the merge (next row) |
| M2-AC01/3 branch protection (after merge, D22) | Not yet applied | GitHub repository settings | An admin, once `app.yml` has run on `main`: `gh api -X PATCH repos/BELCORT-SDN-BHD/wringy/branches/main/protection/required_status_checks -f strict=true -f 'contexts[]=planning' -f 'contexts[]=check' -f 'contexts[]=integration' -f 'contexts[]=e2e'`. `images` stays optional. GitHub's REST reference marks `contexts` deprecated in favour of `checks` (objects with `context` and an optional `app_id`); the equivalent `checks` body pins each context to GitHub Actions (app id 15368, as `planning` is today) | `planning`, `check`, `integration` and `e2e` required; `app.yml` has no `paths:` filter (D22a), so documentation PRs report all four | **NOT EXECUTED**: this needs admin rights and CI green on `main` first; the agent does not change repository settings. Cost: about 15 minutes of admin time (kickoff-package.md §2.2, G5) | Pending | [REST: update status check protection](https://docs.github.com/en/rest/branches/branch-protection?apiVersion=2022-11-28#update-status-check-protection); kickoff-package.md §9.1 D22 | Founder or admin action after merge; re-read the protection afterwards and add a row here |
| M2-AC01/3 cross-vendor review | This PR | — | Founder ruling D23: nothing is enforced; the per-ticket review is the repository's `code-review` skill; the Codex read-only review is at the orchestrator's discretion, saved as a PR comment when it runs | Register what actually ran, never an unrun review | **Run** on 2026-09-23 (Codex CLI 0.153.4, model gpt-6-astra, sandbox read-only) on `12a85ab` against `main`. The result is saved as a PR #82 comment. **Two defects** were found and are fixed: (1) major, a fixture relabel race: `pnpm db:env --relabel` checked for fixture rows and updated the marker in separate, unlocked statements, fixed in `2e84a7a` (SHARE locks on every `data_origin` table before the count; `packages/db/test/environment.int.test.ts` "M2-AC01/2 the production relabel is serialised with fixture writes"); (2) minor, the query string in request logs: Fastify's default `req` serializer logged `req.url` whole, fixed in the commit after `2e84a7a` (a `req` serializer that logs the path only; `apps/api/tests/integration/secrets.int.test.ts` "M2-AC01/2 request logs keep the pathname and drop the query string …"). **Found sound** by static review: database privileges (migrations 0001–0007, bootstrap and migration scripts, grant manifest); secrets (config validation, fixed API error bodies, the canary, Docker context exclusions, no secret build arguments); the API (zod response allow-lists, fixture filtering, readiness against liveness, statement and query timeouts, disposal of broken clients); the worker (`migrate: false`, database-clock heartbeats, the drain, stop-marker and pool-close order); delivery (three non-root images, migration bundle packaging, workflow permissions, build-only behaviour). The reviewer ran no tests, image builds or Render. The W5 adversarial review below is the repository's own review. GitHub enforces no review: 0 required approvals and no `CODEOWNERS` (kickoff-package.md §2.1). Automating the review would cost about 1 day or more and needs Codex credentials usable in Actions (unverified); it stays open as G8 | 2026-09-23 | [PR #82 comment](https://github.com/BELCORT-SDN-BHD/wringy/pull/82#issuecomment-5790041412); [kickoff-package.md](kickoff-package.md) §6.6, §9.1 D23, §10 G8 | None: the fixes ran green in CI on `fe17df4` (run 35828034405) |
| M2-AC01/3 release gate | `feat/m2-01` (W4) | GitHub repository; real | Check for a release gate: GitHub environments, deploy workflows | Report that none exists, without claiming one | **None, by ruling D24**: M2 has no production release. 0 GitHub environments (`gh api repos/BELCORT-SDN-BHD/wringy/environments` → `total_count: 0`, re-read 2026-09-23T00:46:55Z and 05:48:15Z); no deploy workflow: `app.yml` builds the three images with `push: false`, logs in to no registry, and deploys nothing. Building it (a staging GitHub Environment, a deploy workflow of SHA images gated on the required checks) costs about 1–2 days and belongs to M2-09 (G9) | 2026-09-23, W4 worker | [kickoff-package.md](kickoff-package.md) §2.2, §9.1 D24, §10 G9; `.github/workflows/app.yml` | M2-09 |

## Recovery: removing the new entry returns the prototype

`m2-01.md` "Verification and recovery": 移除新入口可回到原型，保留源设计资产 (review finding spec-docs-1).

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC01 recovery: remove the new entry | `2be5188` (the code of every review fix; the script and this record were added after it) | Local, Windows 11, Node 24.21.0, pnpm 10.33.0; a throwaway git worktree of HEAD in the system temp directory, removed afterwards; real | `node scripts/rehearse-prototype-rollback.mjs --e2e`: delete `apps/web/src/app/(internal)/` (the (internal) root layout, the /internal page and its parts: 11 tracked files), check nothing left in `apps/web/src` imports `@wringy/*`, `pnpm install --frozen-lockfile --offline`, `pnpm --filter web test`, `pnpm --filter web build`, `next start` and GET `/`, `/campaigns`, `/internal` and an unmatched URL, then the M1 prototype Playwright suite on a warmed `next dev` without the removed entry's own tests (`--grep-invert M2-AC01`: the suite's "M2-AC01 internal build route walk" in `tests/e2e/i18n.spec.ts` walks /internal, 3 locales in 3 projects; run with them on the same worktree, those 9 failed and the other 318 passed); and, in the checkout, `git diff <merge base> HEAD` over the design sources | No manifest or lockfile edit; tests and build pass; /internal answers exactly as any URL the prototype does not know; the M1 suite passes; the design sources are unchanged | See the run log below the table | 2026-09-23, review-fix worker | `scripts/rehearse-prototype-rollback.mjs` | A deeper back-out that also deletes `apps/api`, `apps/worker` and `packages/*` is not rehearsed. Besides deleting them it needs: `apps/web/package.json` (drop `@wringy/config`, `@wringy/contracts` and the devDependency `@wringy/db`), `apps/web/tests/e2e-internal/`, `apps/web/playwright.internal.config.ts`, the `internal` message namespace (`src/i18n/messages.ts`, `src/messages/*/internal.json`), the root `package.json` scripts (`db:*`, `e2e:internal`, `depcruise`, `canary`, `check:acceptance`), `pnpm-workspace.yaml` (`packages/*`), `.dependency-cruiser.cjs`, `scripts/check-*.mjs`, `.github/workflows/app.yml`, and a lockfile update. Not needed to return to the prototype, which (above) no longer imports any of them once the entry is gone |

Run log (`node scripts/rehearse-prototype-rollback.mjs --e2e`, 2026-09-23):

```text
rollback: design assets unchanged since the merge base 3def287: phase-0/foundation/design-system-v2, phase-0/deliverables/brand, apps/web/src/components/ui, apps/web/src/app/globals.css, apps/web/public
rollback: worktree of 2be5188 in the system temp directory
rollback: removed apps/web/src/app/(internal) (11 tracked files)
rollback: apps/web/src imports no @wringy/* package once the entry is removed
rollback: pnpm install --frozen-lockfile --offline: exit 0 (no manifest or lockfile edit needed)
rollback: pnpm --filter web test: exit 0 (Tests  329 passed (329))
rollback: pnpm --filter web build: exit 0, 37 route lines, none of them /internal
rollback: next start: / 200, /campaigns 200, /internal 404 like any unmatched prototype URL (<title>Wringy — prototype</title><html id="__next_error__">)
rollback: next dev warmed: 36 routes
rollback: M1 prototype suite (apps/web playwright.config.ts, --grep-invert M2-AC01): exit 0, 33 skipped, 318 passed (4.3m)
rollback: PASS on 2be5188
```

The design sources checked are `phase-0/foundation/design-system-v2`, `phase-0/deliverables/brand`,
`apps/web/src/components/ui`, `apps/web/src/app/globals.css` and `apps/web/public`. Under
`next start`, /internal and an unknown URL both answer 404 with Next's error document
(`<html id="__next_error__">`) and the prototype's `<title>`: the demo catch-all
(`src/app/(demo)/[...notFound]/page.tsx`, which the removal does not touch) answers every URL the
prototype does not know that way. Its comment says the 404 renders inside the demo root layout; in
`next start` it did not (observed here; `next dev` not checked).

## M2-AC02 — Google sign-in, session refresh and sign-out (M2-02)

The evidence for [M2-02 / #21](https://github.com/BELCORT-SDN-BHD/wringy/issues/21), acceptance
M2-AC02/1–3 ([m2-spec.md](../planning/specs/m2-spec.md#m2-ac02),
[m2-02.md](../planning/tickets/m2-02.md)); the design contract is
[m2-02-code-review.md](m2-02-code-review.md).

**Every automated row below is `simulated`, and simulated rows cannot close this ticket.** The
spec says so in as many words — 真实测试凭据缺失须列阻塞，模拟结果不能关闭本票 (M2-AC02/3) — and
R15 requires both halves of this section: each simulated group labelled, and every §4.9 `Real` row
present up front, marked **NOT EXECUTED** with its reason. What "simulated" means here is precise:

- the Supabase **Auth server** is a local fake (`apps/web/tests/e2e-internal/fake-auth/server.mts`),
  driven through the real `@supabase/ssr` client by its own contract self-test;
- the **key set** is an ES256 pair generated in the test process (`tests/integration/jwt-support.ts`);
- **Google** is never contacted: no consent screen, no real account, no real scope grant;
- the **session store** is either the stub `auth.sessions` or a stubbed liveness port.

They prove the code's logic. They do not prove that a real Google account signs in, that Google
asks for no YouTube scope, or that a real Supabase session answers the liveness question. Those are
the Real rows, and none of them has been executed.

### Execution (M2-AC02)

| | |
|---|---|
| Recorded | 2026-09-26, W5 review-fix worker, after the M2-02 adversarial review; the rows marked W6 were re-taken by the W6 fix worker after the two independent reviews of the integrated tree |
| Branch | `feat/m2-02` (not merged). Each row below names the commit it was taken on: `91c257a` is the W5 review-fix head, `8899217` the W6 fix head. The W1–W3 commits are `6c27eae..91c257a` |
| Executed by | Agent runs of M2-02 (W1 db/config, W2 api, W3 web, W5 review fixes, W6 review fixes). **No founder step is recorded here** |
| Runtime | Node 24.21.0 (root `.npmrc` `use-node-version`), pnpm 10.33.0; PostgreSQL 17.10 through embedded-postgres locally; Chromium only |
| Migration head | `0010_profiles_column_grants` (`packages/db/src/expected-head.ts`); `0009_sign_in_allowlist` for the rows taken on `91c257a` |
| Commands | `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm depcruise`, `pnpm check:supabase-scope`, `pnpm canary`, `pnpm check:acceptance`, `pnpm --filter @wringy/db test:int`, `pnpm --filter api test:int`, `pnpm --filter worker test:int`, `pnpm e2e:internal`, `pnpm --filter web exec playwright test` |
| Mapping | `pnpm check:acceptance m2-01 m2-02` on `8899217`: M2-AC02/1 56 test names, M2-AC02/2 148, M2-AC02/3 76 (all carrying their ID); plus the rows below |

### Simulated rows (none of these can close the ticket)

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC02/1 simulated sign-in outcomes | `8899217` (W6 head); head 0010 | Local + CI; **simulated**: the local fake Auth server, no Google | `apps/web/tests/e2e-internal/auth.spec.ts` (sign-in walk, cancel, expired flow, wrong browser, not-allowed, disabled), the same page at 1440, 390 and 320 px and `src/app/auth/route-handlers.test.ts`; the outcome copy in `src/lib/auth/outcomes.test.ts` | Every way sign-in can end has a visible, localized result: `cancelled`, `expired`, `wrong_browser`, `session_ended`, `signed_out`, `signed_out_unconfirmed`, `not_allowed`, `disabled`, `unexpected` | Passed. The nine codes each have a title and a description in en-MY, ms-MY and zh-Hans-MY, asserted against the message files. W6 added the sign-in page and the signed-out page at 390 and 320 px in all three locales: no horizontal scroll, the Google button visible, frames `sign-in-<locale>-390.png`, `sign-in-<locale>-320.png`, `signed-out-390.png`, `signed-out-320.png` | 2026-09-26 | `apps/web/tests/e2e-internal/auth.spec.ts`; `src/lib/auth/outcomes.test.ts` | **Simulated**: a real Google cancel and a real consent screen are the Real rows below, which have not run |
| M2-AC02/1 simulated scopes | `91c257a` (W5 head) | Local + CI; **simulated**: the fake records the authorize URL | The fake's `AuthorizeRecord` is read back: provider, `redirect_to` and the scopes asked for | The authorize call asks for no extra scope, so Google is asked for `email profile` only and no YouTube grant | Passed against the fake | 2026-09-26 | `apps/web/tests/e2e-internal/fake-auth/server.mts`; `auth.spec.ts` | **Simulated**: what Google actually asks the person for is Real row 2 (`scopes`), which has not run |
| M2-AC02/2 simulated refresh, isolation and the identity header | `8899217` (W6 head) | Local + CI; **simulated** | `apps/web/src/proxy.test.ts` (refresh writes to both the forwarded request and the response, no-store, the token header can only come from the proxy, a retryable Auth failure is `unexpected`, an abandoned sign-in is not a session that ended, a `getClaims` that throws, the 8 s overall deadline, the compiled matcher, the framing headers); `auth.spec.ts` two browser contexts and two tabs of one context reloading together | A refreshed session reaches the page and the browser together; two contexts never swap identities; a client-supplied `x-wringy-access-token` can never survive in internal mode; two tabs reloading together both stay signed in; a `getClaims` that throws ends the session instead of answering 500; a refresh that passes the 8 s deadline answers `unexpected` and applies no cookie | Passed. The two-tab row found a real defect: reading the token with `getSession()` after `getClaims()` refreshed a second time and burned the other tab's grace, so the proxy now reads the token out of the cookie `getClaims()` updated (`readStoredAccessToken`) | 2026-09-26 | `src/proxy.test.ts`; `tests/e2e-internal/auth.spec.ts` | **Simulated**: two real tabs against a real Supabase session is Real row 3 (`refresh`), which has not run |
| M2-AC02/2 simulated session liveness, both adapters | `8899217` (W6 head); head 0010 | Local + CI; **simulated**: the stub `auth.sessions` and an in-process fake `/auth/v1/user` | `apps/api/tests/integration/session-liveness.int.test.ts` (both adapters, fail-closed statuses, the 3 s timeout, the apikey header, and — W6 — a recording port that answers `live` only when it was handed the command's own client and `pg_stat_activity` says that connection is mid-transaction, for `POST /identity/sign-in` and `POST /me/session/probe`) and `packages/db/test/platform.int.test.ts` | `database` answers from `platform.session_is_live`; `auth_server` maps 200 → live, `session_not_found`/`user_not_found`/`user_banned` → 401 `session.revoked`, and every other answer, timeout or transport error → 503 `session_check_unavailable`, never a 401 | Passed | 2026-09-26 | `apps/api/tests/integration/session-liveness.int.test.ts`; `packages/db/test/platform.int.test.ts` | **Simulated**: neither adapter has been asked about a real Supabase session — Real row 4 (`bridge`), which has not run |
| M2-AC02/2 simulated allow-list gate and profile | `8899217` (W6 head); head 0010 | Local + CI, real PostgreSQL 17; **simulated** identity (locally signed tokens) | `apps/api/tests/integration/identity.int.test.ts`; `packages/db/test/allowlist.int.test.ts`; `packages/db/src/allowlist{,-args}.test.ts` | An unlisted address with no profile is 403 `sign_in.not_allowed`; a listed one creates the profile with the **verified** email, the display name and the sign-in stamp; removing the address signs nobody out; a disabled profile is 403 `account.disabled` | Passed. The gate compares the normal form (**NFC**, trim, lower-case — W6 changed it from NFKC, which folded a ligature and a full-width letter onto the listed ASCII address, so two mailboxes shared one row) while `contact_email` keeps the address as the provider verified it. W6 also moved the profile writes behind column grants (migration 0010): `UPDATE app.profiles SET status …` and an INSERT naming `status` are 42501 for the runtime role, and both commands re-read `status` inside their own transaction, so an account disabled between the hook's read and the commit is refused 403 `account.disabled` | 2026-09-26 | `apps/api/tests/integration/identity.int.test.ts`; `packages/db/test/allowlist.int.test.ts`; `packages/db/test/grants.int.test.ts`; `packages/db/migrations/0010_profiles_column_grants.sql` | **Simulated**: no real address has passed the gate — Real rows 1 and 7, which have not run |
| M2-AC02/2 simulated sign-out and the disabled-account exit | `8899217` (W6 head) | Local + CI; **simulated** | `src/app/auth/route-handlers.test.ts` (sign-out, `signed_out_unconfirmed`, `GET /auth/end-session`) and `auth.spec.ts` (sign-out, back button) | `signOut({scope:'local'})` only; a sign-out the library could not confirm still expires every `sb-*` cookie; the back button after sign-out shows no private data; only `403 account.disabled` ends a session through `/auth/end-session`; and 关闭登录入口时保留退出通道，不删除身份记录 — an account whose sign-in is closed still signs out (303 `signed_out`, no `sb-*` cookie left) and its identity row survives, disabled, because the runtime role has no DELETE on `app.profiles` at all | Passed | 2026-09-26 | `src/app/auth/route-handlers.test.ts`; `tests/e2e-internal/auth.spec.ts` ("simulated exit"); `packages/db/test/profiles.int.test.ts` (DELETE and TRUNCATE are 42501) | **Simulated**: "other devices stay signed in" against a real session is Real row 5 (`scope`), which has not run |
| M2-AC02/3 simulated forged and stale tokens | `91c257a` (W5 head) | Local + CI; **simulated**: an ES256 key pair generated in the process | `apps/api/tests/integration/authenticate.int.test.ts`: unknown key, `alg=none`, HS256 with the publishable key, wrong `iss`/`aud`, missing claims, wrong role, anonymous, `client_id`, unknown `kid`, expired, unreachable JWKS; every route but `/health` and `/health/live` with no token | One 401 `unauthenticated` for every forgery; `auth.expired` for a valid signature past `exp`; 503 `auth_unavailable` (never a 401) when the key set cannot be fetched; no token, session id, email or claim value in any log line | Passed | 2026-09-26 | `apps/api/tests/integration/authenticate.int.test.ts`; `apps/api/src/app.ts` (`routeTable`) | **Simulated**: the tokens are ours, not Supabase's. Real row 9 (`stale`, refresh-token reuse) has not run |
| M2-AC02/3 simulated Origin, CSRF and the forwarded host | `8899217` (W6 head) | Local + CI; **simulated** | `src/lib/auth/origin.test.ts`, `src/app/auth/route-handlers.test.ts`, `auth.spec.ts` ("simulated origin", "simulated callback") | A cross-site POST is 403 before anything upstream is called and writes no cookie; `GET /auth/sign-out` is 405; `next=//evil.example`, `next=https://evil.example` and a forged `X-Forwarded-Host` never leave `APP_ORIGIN`; and every response the proxy produces in internal mode carries `Content-Security-Policy: frame-ancestors 'none'` and `X-Frame-Options: DENY`, so no page of this build can be framed into a click-jacking flow | Passed | 2026-09-26 | `src/lib/auth/origin.test.ts`; `tests/e2e-internal/auth.spec.ts` | **Simulated** |
| M2-AC02/3 simulated cache and shared-cache behaviour | `8899217` (W6 head) | Local + CI; **simulated** | `src/lib/auth/no-store.test.ts`, `route-handlers.test.ts`, `proxy.test.ts`, `auth.spec.ts` cache rows; `apps/api` `CACHE_CONTROL` and `Vary: Authorization` assertions | Every response that writes a cookie is `private, no-store`, and so is every response the proxy produces for an authenticated `/internal` path whether or not it wrote one (R20 rev 3); an RFC 9111 shared cache carries nothing of user A to user B — W6 made A's read through the caching proxy a **refreshing** read, so the response it must refuse to store really carries a session, and added a public page asked for with an expired session cookie (no `Set-Cookie`, no user data); API responses behind the hook vary by `Authorization`, and `/health` does not | Passed | 2026-09-26 | `src/lib/auth/no-store.test.ts`; `tests/e2e-internal/auth.spec.ts` | **Simulated**: repeated against a real shared cache in M2-09 |
| M2-AC02/3 CI checks for this ticket | `8899217` (W6 head) | Local; CI on the PR | `pnpm depcruise` (rule `supabase-client-only-in-auth-lib` with its planted violation), `pnpm check:supabase-scope` (planted module-level client and planted second importer), `pnpm canary` (the three new variables), `pnpm check:acceptance m2-01 m2-02` | A module-level Supabase client, a second importer of `@supabase/*` and a leaked configured value are each rejected by a check that is proved to bite | Passed: 4 planted violations rejected; the scope check rejected both plants; the canary found no configured value in a bundle or a log | 2026-09-26 | `scripts/check-dependency-direction.mjs`; `scripts/check-supabase-client-scope.mjs`; `scripts/check-secret-canary.mjs` | CI on the PR is the authoritative run |
| M2-AC02/2 simulated revoked, from the API's own mouth | `8899217` (W6 head); head 0010 | Local + CI; **simulated** provider, real Fastify and real PostgreSQL 17 | `auth.spec.ts` "simulated revoked": sign in as Alice, read the stored access token with the application's own `readStoredAccessToken`, sign out through the page, then call `http://127.0.0.1:3200` directly with that token | The token is worthless for a command the moment the session is gone, even though its own `exp` is an hour away | Passed: `POST /me/session/probe` and `POST /identity/sign-in` both answer 401 `session.revoked`, while `GET /me` still answers 200 — the documented trade-off that a read relies on the token alone (§4.6) | 2026-09-26 | `apps/web/tests/e2e-internal/auth.spec.ts`; `apps/api/src/session-liveness.ts` | **Simulated**: the real-server half of this is the orchestrator's probe row above; the cookie/browser half of a real session is Real row 5 (`scope`), which has not run |
| M2-AC02/2 simulated no-demo on a dotted path | `8899217` (W6 head) | Local + CI; **simulated** | `src/proxy.test.ts` (the matcher compiled with Next's own `getMiddlewareMatchers`) and `auth.spec.ts` "simulated no-demo: a dotted path" | A path with a dot in it is inside the internal build's routing shape like any other page | Passed. The rev-2 matcher excluded every path containing a dot, so on an internal origin `/campaigns/a.b` and `/foo.bar` reached the demo catch-all; the suffix matcher fixes it and both rows prove it — the compiled regex matches, and the served page is the internal not-found inside the internal layout with no demo toolbar | 2026-09-26 | `apps/web/src/proxy.ts`; `src/proxy.test.ts`; `tests/e2e-internal/auth.spec.ts` | **Simulated**: a routing property, so a real provider would add nothing |

### Real Supabase session rows — orchestrator probe (real project, not a Google sign-in)

On 2026-09-26 the orchestrator minted a session on the **real Supabase dev project** without Google
(admin API: create a throwaway `example.test` user → `generate_link` magic link → `POST /auth/v1/verify`;
the secret key was used only from the orchestrator's script, as the #21 checklist allows) and drove
the API built from `6245eb8` (started on 127.0.0.1:3250 with `apps/api/.env`: the dev project's
`SUPABASE_URL` and publishable key, `SESSION_LIVENESS=auth_server`, the local development cluster at
head 0009). These rows are **real Supabase, simulated user origin**: they prove the token and the
liveness bridge, not the Google consent flow. The throwaway user, its local `app.profiles` row and its
allow-list entry were deleted afterwards; the API log held no token-like string.

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC02/2 real token, real key set: Fastify verifies a token the hosted project issued | `6245eb8`; head 0009 | Local API + the Supabase dev project; **real Supabase session, not Google** | Decode the issued token's header and claims; `GET /me` with it before any sign-in | ES256 with a `kid` in the project's JWKS; `iss = <SUPABASE_URL>/auth/v1`, `aud` and `role` `authenticated`, `aal`, `session_id`, `email` present, `is_anonymous` false, no `client_id`; the API answers with a code that proves verification passed | alg ES256, kid present; iss, aud, role, aal `aal1`, amr `[otp]`, session_id, email all present; is_anonymous false; no client_id; lifetime 3600 s. `GET /me` → **403 `profile.missing`** (a forged or foreign token would have been 401), `Vary: Authorization`, `private, no-store` | 2026-09-26 03:0x +08:00, orchestrator | scratchpad script `real-session-probe.mjs` (prints statuses, claim names and booleans only); this row | The claim set matches `RequiredClaims` in auth-js 2.117.2; a Google session adds amr `oauth` |
| M2-AC02/2 real bridge: the sign-in gate and the reserved command ask the real Auth server | `6245eb8` | as above | Allow-list the probe address locally; `POST /identity/sign-in`; `GET /me`; `GET /internal/campaigns`; `POST /me/session/probe`; `GET /me` with `X-User-Id` of another subject | Sign-in 200 with a profile row (Mechanism B answered live); reads 200; probe `{ ok: true }`; the header is ignored | All as expected: sign-in 200 (profile created with the normalised address), `/me` 200, `/internal/campaigns` 200, probe 200 `ok`, the `X-User-Id` request still answered as the token's subject | 2026-09-26, orchestrator | same script | The browser path (cookies, proxy, the page's probe button) stays with the founder's walk, row "Real bridge" below |
| M2-AC02/2 real revoked: after a real local-scope sign-out the old, unexpired token cannot write | `6245eb8` | as above | `POST <SUPABASE_URL>/auth/v1/logout?scope=local` with the token, then replay it on the probe, on sign-in and on a read | 204 from Supabase; probe and sign-in **401 `session.revoked`** (the real `session_not_found` from `/auth/v1/user`); the read still answers until `exp` (documented, R2/R8) | Logout 204; probe → 401 `session.revoked`; sign-in → 401 `session.revoked`; `GET /me` → 200 | 2026-09-26, orchestrator | same script | This is the real-server half of §4.9 "API-int + Real: revoked"; the cookie/browser half is in the founder's walk |
| M2-AC02/2 real stale (partial): the ended session's refresh token is refused | `6245eb8` | as above | `POST /auth/v1/token?grant_type=refresh_token` with the signed-out session's refresh token | Supabase refuses it and the session stays ended | **400 `refresh_token_not_found`** | 2026-09-26, orchestrator | same script | Covers "a refresh token of an ended session"; the §4.9 "stale" row proper (reuse of a *rotated* token outside the 10 s reuse interval) is still NOT EXECUTED below |

### §4.8 Supabase Auth checklist, per environment

kickoff-package.md §4.8 asks each environment to record its Auth checklist as M2-01/M2-02 evidence. What
the orchestrator could establish from outside the dashboard on 2026-09-25/26, with the check that
established it; anything not listed here is unverified until the founder reads the dashboard (runbook §1).

| Item (§4.8) | dev project | staging project | How it was checked |
|---|---|---|---|
| Only the Google provider is enabled (email, phone, anonymous off) | verified | verified | `GET /auth/v1/settings` with the publishable key: `external` has only `google: true` |
| The signing key is ES256 | verified (one EC P-256 key) | verified | `GET /auth/v1/.well-known/jwks.json` |
| The consent screen lists only the default scopes (no YouTube) | verified: `scope=email profile` | verified: `scope=email profile` | `GET /auth/v1/authorize?provider=google&redirect_to=…` → the `Location` to accounts.google.com |
| Site URL / redirect allow-list accept the local internal build | verified for `http://127.0.0.1:3100/auth/callback`: the W3 smoke's `POST /auth/sign-in` → 303 to `/auth/v1/authorize` → 302 to Google (a refused `redirect_to` answers an error instead) | **unverified** (staging has no deployed origin yet; M2-09) | W3 integration smoke, repeated by the W3 verifier |
| Access-token lifetime is the default 1 hour (D14) | verified: `exp − iat = 3600` on a real token | **unverified** | W4 real-session probe |
| No `sb_secret_` key in any app process | verified by construction: `publishableKeySchema` refuses a secret key or a service_role JWT (`packages/config/src/shared.ts`, `env.test.ts`) | same | unit test |
| The OAuth-server feature and manual identity linking are off | **unverified** (not exposed by `/auth/v1/settings`) | **unverified** | dashboard only |

### Real rows — NOT EXECUTED (the ticket cannot close on the rows above)

Every §4.9 `Real` row, up front, with the reason it has not run. The walk is
[m2-02-real-login-runbook.md](m2-02-real-login-runbook.md) §3; its step numbers are given.

| 验收ID | 版本 | 环境 | 测试或人工步骤 | 期望 | 实际 | 时间 | 证据引用 | 未决项 |
|---|---|---|---|---|---|---|---|---|
| M2-AC02/1 Real login: a real Google sign-in lands on the original page | — | Local internal build + the Supabase dev project + a real Google test account | Runbook step 1: open `/internal?x=1`, sign in with an allow-listed Google test account | Back on `/internal?x=1`, signed in as that address, with the fixture data | **NOT EXECUTED**: no verified way exists to automate a real Google sign-in in Playwright (G20), and the walk is the founder's — it needs a real Google account and the browser | — | runbook §3 step 1 (`real-01-login.png`) | Blocks closing M2-02 |
| M2-AC02/1 Real scopes: Google asks for no YouTube grant | — | as above | Runbook step 2: read Google's own consent screen | Name, email address and profile only; no YouTube item | **NOT EXECUTED**: the same walk. The agent verified server-side that Supabase's authorize redirect asks for `scope=email profile` (kickoff §1), which is not the same as seeing the consent screen | — | runbook §3 step 2 (`real-02-consent.png`) | Blocks closing M2-02 |
| M2-AC02/2 Real refresh: two tabs refresh together and both stay signed in | — | as above | Runbook step 3 | Both tabs stay signed in; no bounce to the sign-in page | **NOT EXECUTED**: needs a real session with a real refresh token | — | runbook §3 step 3 (`real-03-two-tabs.png`) | Blocks closing M2-02 |
| M2-AC02/2 Real bridge: liveness answers against the real auth.sessions | — | as above, `SESSION_LIVENESS=auth_server` | Runbook step 4: the session probe on `/internal` | `probe=ok`, proving Fastify asked the real Supabase Auth server about this session | **NOT EXECUTED**: only the fake `/auth/v1/user` and the stub table have been asked so far | — | runbook §3 step 4 (`real-04-probe-ok.png`) | Blocks closing M2-02. Mechanism A against a real `auth.sessions` waits for M2-09 |
| M2-AC02/2 Real scope: local sign-out leaves the other device signed in | — | as above, two browsers | Runbook step 5 | Browser A is signed out; browser B stays signed in and its probe still answers ok | **NOT EXECUTED**: needs two real sessions of one real account | — | runbook §3 step 5 (`real-05a`, `real-05b`) | Blocks closing M2-02 |
| M2-AC02/3 Real cancel and not_allowed: a refusal is visible and neutral | — | as above, plus a real Google account that is **not** allow-listed | Runbook steps 6 and 7 | "Sign-in cancelled"; and a neutral internal-version page with no data for the unlisted account | **NOT EXECUTED**: the same walk | — | runbook §3 steps 6–7 (`real-06-cancelled.png`, `real-07-not-allowed.png`) | Blocks closing M2-02 |
| M2-AC02/3 Real logout: the back button shows no private data | — | as above | Runbook step 8 | No signed-in header and no campaign data after going back | **NOT EXECUTED**: the same walk (the simulated E2E row covers the logic) | — | runbook §3 step 8 (`real-08-back.png`) | Blocks closing M2-02 |
| M2-AC02/3 Real stale: a reused refresh token ends the session | — | as above | §4.9 `Real` "stale": reuse a *rotated* refresh token outside the 10 s reuse interval | The session ends | **NOT EXECUTED**: it needs a script that captures a real refresh token, which a person cannot do by hand. What exists: the fake server's rotation and reuse-window rule is exercised by the harness self-test (`tests/unit/fake-auth-contract.test.ts`, simulated), and the orchestrator's real-session probe showed the real project refusing an *ended* session's refresh token (row "real stale (partial)" above). No API integration test touches refresh tokens | — | runbook §3 step 9 | Blocks closing M2-02 like the other Real rows; the capture script is a small follow-up (the probe script already holds a real refresh token, so the extension is one rotate-then-reuse step) |
| M2-AC02/1 Real locales: the sign-in page in three languages | — | as above | Runbook step 10 | The sign-in page reads correctly in en-MY, ms-MY and zh-Hans-MY | **NOT EXECUTED**: the simulated suite asserts the copy in all three; a human has not looked at the real page | — | runbook §3 step 10 (`real-10-ms.png`, `real-10-zh.png`) | Blocks closing M2-02 |

### Amendment to an M2-AC01 row (M2-02 R15)

| Row | What changed |
|---|---|
| "M2-AC01 failure and refusal states" (above) | Its two not-found tests recorded, in 实际, that the browser shows "the internal not-found page, **or the demo layout**". Since M2-02 the second half cannot happen on an internal origin: `proxy.ts` rewrites every path outside `/internal`, `/internal/…` and `/auth/…` to the internal not-found page (R12), so `internal.spec.ts`'s row was retitled from "unmatched URL outside /internal renders the demo root layout" to "…renders the internal not-found page, never the demo root layout" and now asserts the internal banner and the absence of the demo toolbar. The M2-AC01 expectation (a 404 inside the right root layout, with `lang`, the CSS and the banner) is unchanged and still passes; only the alternative is gone. Recorded here because R15 requires the change to be in the evidence |

## W5 adversarial review

On 2026-09-23 the orchestrator ran an adversarial review of `git diff 3def287...9ca00a5`: reviewers
in six areas (security and privileges, runtime correctness, test integrity, CI/Docker/operations,
web/locale/accessibility, spec and docs) proposed findings, and refuters tried to disprove each one.
It is the repository's own review, not a cross-vendor one (see the M2-AC01/3 cross-vendor row).

Outcome: 29 findings proposed, **23 confirmed** (2 blocker, 5 major, 16 minor) and 6 rejected. All 23
were fixed, and so was one gap found while fixing (no dependency-cruiser rule kept apps/worker from
`@wringy/contracts`, kickoff-package.md §8.1 and §8.7); none was skipped. The fixes are the 18
commits "Fix M2-01 review: …" from `18099af` to `9615439` on top of `9ca00a5`, run through the gates
in "Review-fix gate run" below; they ran green in CI on `12a85ab` (run 35825491353) and, with the cross-vendor fixes, on `fe17df4` (run 35828034405).

| Finding | Severity | What was wrong | Fix | Commit | Test that now holds it |
|---|---|---|---|---|---|
| security-privileges-1 | blocker | The worker's DML rights on `pgboss` let it steer the DDL the migrator runs (reproduced) | Migration `0006_pgboss_runtime_bounds`: the worker keeps SELECT on `pgboss.version` and UPDATE only on its run-time timestamp columns; a CHECK keeps every queue on the shared, unpartitioned `job_common`; `installPgBossSchema()` refuses to start the pg-boss CLI while any queue row breaks that (`PgBossQueueRefusedError`, a count only), which covers databases migrated before 0006 | `18099af` | `packages/db/test/pgboss-bounds.int.test.ts`: 42501 on the version, 23514 on a planted queue; the review's reproduction replayed on a clone reverted to 0005 is refused before the CLI runs, with no index created |
| security-privileges-4 | major | The API login had `pgboss` access, which §4.11 and §8.5 forbid; the deviation was not recorded | The migrator-owned, non-updatable view `ops.pgboss_schema_version`; the API reads the version there and has nothing in `pgboss`; the §4.11/§8.3 reconciliation is in "Deviations" below | `18099af` | `grants.int.test.ts` "…the API has nothing in pgboss"; `runtime-role.int.test.ts` (42501 on `pgboss.version`, the view readable, not updatable 55000); `health.int.test.ts` (rolled back to 0005: the queue check fails with 42P01, logged) |
| security-privileges-5 | minor | The grant-manifest test could not see column grants or grants outside `app`, `ops` and `pgboss` | Column entries in the manifest; the scan covers every non-system schema, `public` included, and column ACLs | `651bc2c` | `grants.int.test.ts` "M2-AC01/2 runtime role privileges match reviewed grant manifest" and the ACL test (fails with the `cron_on` entry removed) |
| correctness-runtime-7 | minor | The fixture trigger checked only NEW, so a fixture row could be relabelled live even in a production-marked database | Migration `0007_data_origin_immutable`: a BEFORE UPDATE trigger refuses any `data_origin` change (23514) | `b2c1fb5` | `fixture-guard.int.test.ts` "M2-AC01/2 a row's data origin never changes…" |
| correctness-runtime-6 | minor | `pnpm db:env --relabel` to production kept fixture rows, which the production API would then serve | `setEnvironment` refuses a marker that forbids fixtures while any table with `data_origin` holds fixture rows (`FixturesPresentError`, counts per table) | `37a7c2d` | `environment.int.test.ts` "M2-AC01/2 refuses to mark a database production while it holds fixture rows, and allows it once they are deleted" |
| security-privileges-2 | major | `db:bootstrap` applied the public local-development passwords to any cluster when `WRINGY_ENV=local` | The development passwords only for the embedded cluster (admin URL unset, or loopback at the embedded port); elsewhere all three passwords are required and a development value is refused (`DevelopmentPasswordRefusedError`, names only) | `a65a342` | `packages/config/src/env.test.ts`; `packages/db/src/bootstrap-plan.test.ts` (5 cases). Probe: `WRINGY_ENV=local` with a remote admin URL exits 1 before connecting |
| correctness-runtime-8 | minor | Same hole as security-privileges-2, found from the runtime side | Same fix | `a65a342` | As above |
| test-integrity-2 | major | The test harness reset cluster-wide login passwords on any `TEST_DATABASE_URL` cluster | `assertThrowawayCluster()` before bootstrapping: a non-loopback host needs `WRINGY_TEST_CLUSTER_IS_THROWAWAY=1`, and a cluster where another database is marked other than `local` or `ci` is refused | `36b8d50` | `packages/db/test/cluster.int.test.ts` (host refusal; marker refusal) |
| security-privileges-3 | minor | Same hole as test-integrity-2 | Same fix | `36b8d50` | As above |
| test-integrity-9 | minor | No test asserted the PostgreSQL or Node versions of the supported intersection | The harness refuses a Node major other than `.nvmrc`'s and a server major other than the pinned one | `922c1ca` | `cluster.int.test.ts` "runs on PostgreSQL 17 and Node 24", "refuses PostgreSQL 16 and Node 20" |
| correctness-runtime-1 | major | The API had only a client-side `query_timeout`, so timed-out reads left server backends running past the pool limit | `statement_timeout` 4.5 s as a startup parameter on the API pool, below the 5 s client limit; a client is discarded only when the connection is unusable | `4f63923` | `apps/api/tests/integration/timeouts.int.test.ts`: under a held lock, three rounds of 10 reads end 503 in [4.25 s, 7 s) with 0 backends waiting (10 without the setting) |
| correctness-runtime-2 | minor | `/health` returned a stuck client to the pool after a timeout in its second or third check | A check that leaves the connection unusable ends the remaining checks and the client is discarded | `4f63923` | `timeouts.int.test.ts`: behind a TCP proxy that swallows replies, `/health` ends in [4.75 s, 10 s) and the next `/health` still reads the database |
| correctness-runtime-3 | minor | A graceful stop waited without limit for a stuck beat before the pg-boss drain started | The drain starts at once beside the settling beat; the worker pool gets a 4.5 s statement and 5 s query limit | `27de11a` | `apps/worker/test/heartbeat.int.test.ts`: a beat under a lock fails 57014 in [4.25 s, 6 s); pg-boss stops within 3 s while a beat is stuck |
| test-integrity-1 | major | `check:acceptance` counted skipped tests as mapping evidence | A Playwright spec whose every test is expected skipped no longer counts; verdict (c) refuses an unconditional run-time skip in a listed file | `f16c95e` | The self-test and the replay in the "M2-AC01/3 CI checks (review fixes)" row |
| test-integrity-3 | minor | The only test mapped to M2-AC01/3 tested the harness, and the mapping check had no self-test | Every run first checks its parser against fixtures with known verdicts; the harness test is retagged `M2-AC01` | `f16c95e` | `scripts/fixtures/acceptance-self-test/` |
| test-integrity-4 | minor | Test names claimed timeout behaviour but asserted only constants | `readInternalApi` takes a timeout and is tested against a server that never answers and one that stalls the body; the API pool test is renamed to say it checks configuration | `e35a273` (web), `4f63923` (api) | `apps/web/src/app/(internal)/internal/api-read.test.ts` L88, L108; `timeouts.int.test.ts` |
| test-integrity-5 | minor | API-level tests were titled "page→Fastify→PostgreSQL" but involve no page | Retitled "M2-AC01/2 Fastify→PostgreSQL read (API leg): …"; the api README names the Playwright cold start as the page leg | `e35a273` | `internal-campaigns.int.test.ts`, `worker-health.int.test.ts` |
| test-integrity-7 | minor | API integration tests shared committed state, so one leak probe was partly vacuous alone | Each test that needs the canary column adds it itself, and the probe asserts the selected rows carried it; the §6.3 deviation is in "Deviations" below | `e35a273` | `vitest -t 'serializer strips'` passes alone |
| test-integrity-8 | minor | CI retries hid flaky internal e2e tests; the outage test relied on an implicit pool default | `failOnFlakyTests: !!process.env.CI`; the pool's idle timeout is exported and the outage test polls at it plus 1 s | `ddfd33e` | `apps/api/src/database.test.ts`; in CI the `e2e` jobs of runs 35825491353 and 35828034405 ran the internal suite with `failOnFlakyTests` active and passed |
| test-integrity-6 | minor | The scheduled (cron) queue round trip was never asserted | A test runs the worker as `main.ts` builds it, sends nothing and waits for the scheduled round trip | `e3d71ae` | `apps/worker/test/schedule.int.test.ts` (6.8 s locally) |
| (gap found while fixing) | — | No rule kept apps/worker from importing `@wringy/contracts` (kickoff-package.md §8.1, §8.7) | Rule `worker-not-to-contracts`; `pnpm depcruise` plants every fixture in one cruise and requires each rule to name its file. No other §8.7 row is missing (checked row by row) | `bc1f7fd` | `pnpm depcruise`: both planted violations rejected |
| ci-docker-ops-1 | minor | `pnpm dev` left apps/api and apps/worker silent on Windows | The api and worker `dev` scripts run `node --watch --import tsx` instead of `tsx watch`; `pnpm dev` stays `pnpm -r --parallel` (D32); the §8.8 deviation is in "Deviations" below | `2be5188` | Manual only: `pnpm dev` printed 7 api, 7 worker and 7 web lines in 25 s (commit message of `2be5188`); no automated test |
| spec-docs-2 | minor | ARCHITECTURE §4's README menu omitted two of the five new READMEs | The config and contracts READMEs listed in §4 and linked from §1 | `a4a9a7d` | Documentation only |
| spec-docs-1 | blocker | The ticket's "移除新入口可回到原型，保留源设计资产" was never demonstrated | `scripts/rehearse-prototype-rollback.mjs [--e2e]`, run once: PASS | `de40e4f` (script and record), `9615439` (record wording) | "Recovery" above |

Rejected in review (refuted, not fixed): security-privileges-6 (the API image listens on 0.0.0.0 and
is documented for a Render web service while `/internal/*` has no authentication);
correctness-runtime-4 (a draining old process marks the row of a newer process with the same
`WORKER_ID` as stopped); correctness-runtime-5 (with several workers `queueState` is structurally
wrong); correctness-runtime-9 (`/health` demands exact equality with the migration head);
web-locale-a11y-1 (the campaigns table's horizontal-scroll container is not keyboard-focusable);
spec-docs-3 (nothing stops `packages/config` and `packages/contracts` importing each other). This
record lists them by title only: the refutation reasoning was not handed to it.

## Deviations from the signed kickoff text

M2-01 follows kickoff-package.md except where a row below says otherwise. The signed sections are
not edited; each deviation is recorded here and in the module README or note named.

| Signed text | What the build does | Why | Where |
|---|---|---|---|
| §8.5 one root `.env.example` | Three names-only examples: the root one (web, migrate, bootstrap, test) plus `apps/api/.env.example` and `apps/worker/.env.example`, each app loading its own gitignored `.env` | The API and the worker both read a variable named `DATABASE_URL`, each with its own login; file locations belong to the code review (`m2-spec.md` L41), so this is an engineering choice, not a founder ruling. `config.test.ts` in each app fails if its example and its env schema name different variables, or the example carries a value | A dated implementation note under §8.5 (kickoff-package.md L666; the signed wording is in `3def287`), added in `c30da02` (W2 fix) |
| §6.3 "API integration tests each run in a transaction that is rolled back afterwards" | Each API integration test file gets its own committed clone; a test that needs a state arranges it itself (the leak probe adds its own canary column) | The app under test reads through its own `pg.Pool` as `wringy_api_login`, which cannot see an uncommitted transaction opened by the test on another connection; packages/db's own tests, which query directly, do use `withRollback` | `apps/api/README.md` "Integration tests"; review finding test-integrity-7 |
| §8.8 "api on port 3200 (through `tsx watch`)" | `apps/api` and `apps/worker` run `node --watch --import tsx` in `dev`; `pnpm dev` stays `pnpm -r --parallel` (D32) | `tsx watch` started by `pnpm -r --parallel run dev` printed nothing on Windows (0 lines in 12 s on 2026-09-23; `node --watch --import tsx` 4 lines in 8 s, and `pnpm dev` 7 api, 7 worker and 7 web lines in 25 s) | root `README.md`; commit `2be5188`; review finding ci-docker-ops-1 |
| §8.4 lists migrations 0001–0005 | Adds `0006_pgboss_runtime_bounds` and `0007_data_origin_immutable` | Review findings security-privileges-1 and -4 and correctness-runtime-7. New migrations rather than edits of 0005/0002: node-pg-migrate does not re-run an edited file on a database that already applied it, so an edit would silently leave such a database on the old grants | `packages/db/README.md` "Schema (M2-01)" |
| §4.11 "No `pgboss` access" and §8.5 "no `pgboss`" for the API login, next to §8.3 "`GET /health` queries the database as the runtime role and compares … the pg-boss schema version" | Both hold: the API login has no right in schema `pgboss` and reads the version, as the runtime role, through the migrator-owned view `ops.pgboss_schema_version` (0006) | 0005 had granted the API USAGE on `pgboss` and SELECT on `pgboss.version` without recording the conflict between the two signed sentences | `apps/api/README.md` `GET /health`; review finding security-privileges-4 |
| §4.11 "A CI test compares `has_table_privilege` for every role against the checked-in manifest" | Stricter: the manifest test also compares column-level privileges and covers every non-system schema, `public` included | A column GRANT or a grant in a new schema passed the table-level check | `packages/db/test/grant-manifest.ts`; review finding security-privileges-5 |

## W0–W3 gate runs

Run by the W0–W3 workers on 2026-09-23, Windows 11, Node 24.21.0, pnpm 10.33.0, each on the commit
named. Local only; CI did not exist before W4 (at `fb46d3c` the W3 worker found no `.github` change
since `c30da02`).

| Wave | Commit | Result |
|---|---|---|
| W0 | `5ab89e1` | web lint 0 findings, typecheck ok, 322 unit tests (24 files), build ok, M1 e2e 318 passed, 33 skipped, 0 failed, all under Node 24.21.0 |
| W1 | `d59a007`, `5de916d` | Root `pnpm lint` and `pnpm typecheck` exit 0 (4 workspaces); unit config 15, contracts 9, db 8, web 325; build exit 0; db test:int 13 passed; depcruise PASS (303 modules) with the planted violation rejected; M1 e2e 327 passed (318 + 9 new), 33 skipped |
| W2a (db) | `d60f579` | lint and typecheck clean; db unit 23, db test:int 39 (7 files); depcruise PASS with the planted violation rejected; planning "Validated 5 specifications and 60 tasks; dependencies acyclic." |
| W2b (api) | `e5a0745` | lint, typecheck and unit exit 0 at the root and for api; api test:int 24 passed, 1 skipped (SIGTERM on win32); depcruise PASS (328 modules); `node dist/main.js` listened on 127.0.0.1:3200 after "environment marker matches" |
| W2c (worker) | `9001493` | lint, typecheck, test, test:int, build exit 0; worker unit 23, worker test:int 12 (and 12 in 3 reruns), db test:int 39, web 325; worker bundle 758.0 KiB with externals pg, pg-boss and pino only; depcruise PASS (328 modules, 1306 dependencies) |
| W2 merged | `d78ef67` | lint, typecheck, test, test:int, build, depcruise exit 0. Unit: web 325, api 36, worker 23, db 23, config 15, contracts 9. Integration: db 39, api 24 + 1 skipped, worker 12. depcruise 344 modules, planted violation rejected. Planning 5 specs, 60 tasks, acyclic; `git diff --check` clean |
| W2 fix | `c30da02` | All exit 0. Unit: web 325, api 36, worker 24, db 23, config 15, contracts 9. Integration: db 39, api 24 + 1 skipped, worker 12. depcruise 345 modules. M1 e2e 327 passed, 33 skipped |
| W3 | `fb46d3c` | lint, typecheck, test, test:int, build exit 0 (api test:int 25 + 1 skipped; api unit 41, db unit 25); depcruise 359 modules, 1397 dependencies; canary PASS; `pnpm e2e:internal` 28 passed, 4 skipped, three runs; the M1 suite 327 passed, 33 skipped (run at `5560a4e`), tree clean afterwards |
| W3 fix | `894d8a1` | lint, typecheck, test (web 334), test:int, build, depcruise (planted import rejected, exit 1), canary PASS (0 hits), M1 e2e 327 passed, 33 skipped, `git diff --check` clean, planning 5 specs and 60 tasks; `pnpm e2e:internal` 28 passed, 4 skipped, three runs |

## W4 gate run

Run by the W4 worker on 2026-09-23 on `feat/m2-01` at `f2b9f88` (the tree of every W4 code
commit), Windows 11, Node 24.21.0, pnpm 10.33.0. Local only; the CI run is the orchestrator's.

| Gate | Command | Result |
|---|---|---|
| Lint | `pnpm lint` | exit 0 |
| Types | `pnpm typecheck` | exit 0 |
| Unit | `pnpm test` | exit 0: contracts 9, config 15, db 25, worker 25, api 42, web 334 (26 files) passed |
| Integration, embedded clusters | `pnpm test:int` (no `TEST_DATABASE_URL`) | exit 0: db 40 passed (8 files), api 25 passed and 1 skipped (the SIGTERM test skips on win32), worker 12 passed |
| Integration, one shared cluster (the CI shape) | `TEST_DATABASE_URL=<pnpm db:start superuser URL> pnpm test:int`, three runs | exit 0 each, same counts; no test database left behind. Before the bootstrap lock the first of two runs failed with "tuple concurrently updated" (XX000) |
| Build | `pnpm build` | exit 0; `apps/web/.next/standalone/apps/web/server.js`, `packages/db/dist/migrate.js`, `apps/api/dist/main.js`, `apps/worker/dist/main.js` written |
| Dependency direction | `pnpm depcruise` | PASS: no violation in 359 modules and 1399 dependencies; the planted `apps/web/src/lib/__dependency-violation__.ts` rejected by `web-not-to-server-runtime` |
| Secret canary | `pnpm canary` | PASS: 0 canary values in `.next/static` (72 files), `.next/server` (756), the rest of `.next` (1727, standalone included), both bundles, and 10 s of api and worker logs |
| Acceptance mapping | `pnpm check:acceptance` | PASS: 214 names from 10 sources carry `M2-AC`; M2-AC01/1 by 1 row, /2 by 82 tests, /3 by 1 test and 7 rows. It fails on a removed tag and on a removed row (see the M2-AC01/3 CI-checks row) |
| M1 demo suite | `pnpm --filter web e2e`, after warming all 38 routes on `pnpm --filter web dev` (8 s) | 327 passed, 33 skipped (viewport scoping), 0 failed, 4.1 min |
| Internal-build suite | `pnpm e2e:internal` | 28 passed, 4 skipped (viewport scoping), 0 failed, 46 s, embedded cluster; again with `TEST_DATABASE_URL` at the `pnpm db:start` cluster (the CI shape): 28 passed, 4 skipped, 39 s. `next start` warns that it "does not work with output: standalone" and serves as before |
| Planning | `python -X utf8 scripts/check-planning.py` | "Validated 5 specifications and 60 tasks; dependencies acyclic." |
| Workflow lint | actionlint 1.7.12 with shellcheck 0.11.0 (release binaries) on `app.yml` and `planning.yml` | no finding |
| Whitespace | `git diff --check` | clean |

Not run here, and why: the `images` job and every Docker build (no Docker on this machine); the
`integration` and `e2e` jobs on Linux with a `postgres:17` service (no Linux runner here). The
images' contents were exercised instead from local `pnpm deploy` outputs on Node 24.21.0 against
a freshly migrated database (`node dist/migrate.js` from zero and again, the api's `/health`
"ok", the worker's "worker started") and the standalone web server (`/internal` 200).

## Review-fix gate run

Run by the review-fix worker on 2026-09-23 on `feat/m2-01` with the code of `2be5188` (the last
code commit of the review fixes; the rehearsal script and this record's rows came after it),
Windows 11, Node 24.21.0, pnpm 10.33.0. Local only; the CI run is the orchestrator's.

| Gate | Command | Result |
|---|---|---|
| Lint | `pnpm lint` | exit 0 |
| Types | `pnpm typecheck` | exit 0 |
| Unit | `pnpm test` | exit 0: contracts 9, config 17, db 36, web 336 (26 files), worker 25, api 48 passed |
| Integration, embedded clusters | `pnpm test:int` (no `TEST_DATABASE_URL`) | exit 0: db 49 passed (9 files), worker 15 passed (5 files), api 28 passed and 1 skipped (8 files; the SIGTERM test skips on win32) |
| Build | `pnpm build` | exit 0; the migrate image step copies `packages/db/migrations`, so 0006 and 0007 ship with it (`apps/api/Dockerfile`) |
| Dependency direction | `pnpm depcruise` | PASS: no violation in 362 modules and 1409 dependencies; both planted violations rejected, each by its rule (`web-not-to-server-runtime`, `worker-not-to-contracts`) |
| Secret canary | `pnpm canary` | PASS: 0 canary values in the bundles, the server build and 10 s of api and worker logs |
| Acceptance mapping | `pnpm check:acceptance` | PASS, self-test first: 250 names from 10 sources carry `M2-AC`; M2-AC01/1 by 1 row, /2 by 105 tests, /3 by the rows above; no unconditional run-time skip in 45 listed files |
| Internal-build suite | `pnpm e2e:internal` | 28 passed, 4 skipped (viewport scoping), 0 failed, 58 s, embedded cluster |
| M1 demo suite | `pnpm --filter web e2e` on a `next dev` warmed over all 38 routes | 327 passed, 33 skipped, 0 failed, 4.3 min, after `apps/web/.next/dev` (Turbopack's development cache, 4.4 GB) was deleted. Earlier attempts timed out on navigation: the first (180 failed) because the gate helper ran the suite with a blocking `spawnSync` and the dev server stalled on its full stdout pipe (a harness fault, fixed); the next three (13, 15 and 155 failed) while another project's Playwright suite was running on this machine, the last of them with the dev server logging "Finished filesystem cache database compaction in 26.5s". At that time the base commit `9ca00a5`, in a fresh worktree, failed the same two spec files (18 failed), so the timeouts were not from the review fixes. The cause of the 13 and 15 is not established beyond load (see `apps/web/README.md`) |
| Recovery rehearsal | `node scripts/rehearse-prototype-rollback.mjs --e2e` | PASS (the "Recovery" section above) |
| Planning | `python -X utf8 scripts/check-planning.py` | "Validated 5 specifications and 60 tasks; dependencies acyclic." |
| Whitespace | `git diff --check` | clean |

Not run here, and why: the CI-only jobs (`integration` and `e2e` on Linux with a `postgres:17`
service, `images` with Docker), as in the W4 gate run.

## Limitations of this record

- **Internal build, not business acceptance.** Nothing here accepts a business rule, a payout or a
  release. The founder has not walked this build; the founder's walk is M2-AC10 (D27), in M2-10.
- **Three CI heads, one documentation commit after.** CI is green on `9ca00a5`, `12a85ab` and
  `fe17df4`; the Linux-only paths the review fixes add (the timeouts, the scheduled round trip, the
  harness guards, the PostgreSQL 17 and Node 24 assertions, `failOnFlakyTests`) ran there. The commit
  that writes these CI rows is documentation only, so its own run is cited on PR #82 and #16 rather
  than here.
- **Local evidence is Windows only.** Real SIGTERM paths and the images are proven only in CI
  (`ubuntu-24.04`); nothing ran against Supabase or Render.
- **Raw logs are not tracked.** The W0–W3 command logs lived in the session scratchpad; the rows
  cite what reproduces them (test names, commands, commits), and the CI logs are linked by run URL.
- **Chromium only, three viewports**, as in M1 (kickoff-package.md §10 G14, ruling D26).
- **One cross-vendor review ran** for this PR, on `12a85ab` (D23, at the orchestrator's discretion).
  Its two defects are fixed; see the M2-AC01/3 cross-vendor row.
- What the build deliberately does not do, and the accepted limitations, are in
  [known-issues.md](known-issues.md).
