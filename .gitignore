# Local/runtime data
node_modules/
# Embedded PostgreSQL data directory (pnpm db:start)
.local/
.next/
dist/
.env
.env.*
!.env.example
*.pem
*.key
.DS_Store
*.log
coverage/
playwright-report/
test-results/
# Local mirror and generated/reference assets remain outside the repo
sources/

# graphify: keep graph.json, GRAPH_REPORT.md, graph.html, labels, manifest, memory; drop regenerable caches, dated backups
# and the per-machine cost tracker (upstream marks cost.json local-only)
graphify-out/cache/
graphify-out/converted/
graphify-out/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/.graphify_learning.json
graphify-out/reflections/
graphify-out/.graphify_root
graphify-out/.graph.html.stale
graphify-out/cost.json

# apps/web (M1 prototype) generated test artefacts
apps/web/tests/e2e/__screenshots__/
apps/web/playwright-report/
apps/web/test-results/
apps/web/tests/e2e/test-results/

# pnpm depcruise plants this file for one cruise and always deletes it
apps/web/src/lib/__dependency-violation__.ts

# Claude Code agent worktrees (local only)
.claude/worktrees/
graphify-out/.graphify_python
